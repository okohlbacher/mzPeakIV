---
phase: 05-worker-offload-robustness-static-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - src/worker/protocol.ts
  - src/reader/types.ts
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "npm run build exits 0 with no TypeScript or Vite errors"
    - "LoadStage union includes 'no-imaging' as a valid terminal state"
    - "WorkerRequest and WorkerResponse discriminated unions are the only types crossing the Worker boundary"
    - "vite.config.ts declares worker.plugins: () => [wasm(), topLevelAwait()] so production Worker builds include WASM transforms"
  artifacts:
    - path: "src/worker/protocol.ts"
      provides: "Typed postMessage protocol for Worker boundary — WorkerRequest, WorkerResponse, LoadResult, NonImagingResult"
      exports: ["WorkerRequest", "WorkerResponse", "LoadResult", "NonImagingResult"]
    - path: "src/reader/types.ts"
      provides: "LoadStage union extended with 'no-imaging'"
      contains: "'no-imaging'"
    - path: "vite.config.ts"
      provides: "worker.plugins factory for WASM in Worker builds"
      contains: "worker.plugins"
  key_links:
    - from: "vite.config.ts"
      to: "src/worker/mzPeakWorker.ts"
      via: "worker.plugins: () => [wasm(), topLevelAwait()]"
      pattern: "worker\\.plugins"
    - from: "src/worker/protocol.ts"
      to: "src/state/store.ts"
      via: "WorkerRequest / WorkerResponse imports"
      pattern: "WorkerRequest|WorkerResponse"
---

<objective>
Lay the foundational contracts that all subsequent Worker plans depend on: (1) extend vite.config.ts with the worker.plugins factory so WASM imports work in production Worker bundles, (2) create src/worker/protocol.ts with the typed discriminated-union message protocol, and (3) add 'no-imaging' to the LoadStage union in src/reader/types.ts.

Purpose: Without the vite.config.ts fix, WASM silently breaks in production Worker builds (works in dev only). Without protocol.ts, the Worker implementation (Plan 02) and store integration (Plan 03) have no shared type contract. Without the LoadStage extension, TypeScript compile fails the moment 'no-imaging' appears in any switch/Record.

Output: vite.config.ts updated, src/worker/protocol.ts created, src/reader/types.ts extended. These are the foundational contracts — no runtime behavior changes yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-worker-offload-robustness-static-deploy/05-CONTEXT.md
@.planning/phases/05-worker-offload-robustness-static-deploy/05-RESEARCH.md
@.planning/phases/05-worker-offload-robustness-static-deploy/05-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/reader/types.ts (current LoadStage — ADD 'no-imaging' before 'error'):
```typescript
export type LoadStage =
  | "idle"
  | "zip-index"
  | "manifest"
  | "metadata"
  | "grid"
  | "tic"
  | "ready"
  | "error";
```

From src/reader/errors.ts:
```typescript
export type ReaderErrorClass = "unsupported-encoding" | "corrupt";
```

From src/reader/types.ts (types reused in protocol.ts):
```typescript
export type UnsupportedFinding = { code: string; label: string };
export type Capabilities = { layout: "point" | "chunked" | "mixed"; encodings: string[]; isImaging: boolean; unsupported: UnsupportedFinding[]; };
export type FileMeta = { fileDescription: unknown; instrumentConfigurations: unknown[]; software: unknown[]; run: unknown; samples: unknown[]; };
export type FileStats = { numSpectra: number; numEntities: number; mzRange: [number, number] | null; msLevels: number[]; representationCounts: { profile: number; centroid: number }; };
export type ManifestEntry = { name: string; entityType: string; dataKind: string; };
export type SpectrumArrays = { index: number; id: string; mz: Float64Array; intensity: Float32Array; };
```

From src/imaging/types.ts (check actual export name):
ImagingGrid type needed for LoadResult.grid field.

From vite.config.ts (current — ADD worker block):
```typescript
export default defineConfig({
  base: BASE,
  plugins: [react(), wasm(), topLevelAwait()],
  resolve: { alias: { mzpeakts: mzpeaktsSrc } },
  build: { target: "es2022", assetsInlineLimit: 0 },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LoadStage and add worker.plugins to vite.config.ts</name>
  <files>src/reader/types.ts, vite.config.ts</files>
  <read_first>
    - src/reader/types.ts — full file (know current LoadStage union exactly)
    - vite.config.ts — full file (know current defineConfig shape exactly before editing)
    - .planning/phases/05-worker-offload-robustness-static-deploy/05-RESEARCH.md §Pattern 1 — vite.config worker.plugins example
    - .planning/phases/05-worker-offload-robustness-static-deploy/05-PATTERNS.md §LoadStage — exact placement of 'no-imaging'
  </read_first>
  <action>
    In src/reader/types.ts: add "no-imaging" to the LoadStage union BEFORE "error" (per D-06 — non-error terminal states before error). Keep the JSDoc comment on the "tic" line. The union becomes: "idle" | "zip-index" | "manifest" | "metadata" | "grid" | "tic" | "ready" | "no-imaging" | "error". Add a JSDoc comment on the new member: // D-06: valid non-imaging file — successful read, no spatial coords.

    In vite.config.ts: add a `worker` block to the defineConfig object AFTER the existing `build` block. The block is:
      worker: {
        plugins: () => [wasm(), topLevelAwait()],
      }
    Do NOT add `format: 'es'` — leave the format at its default (iife) for Firefox compatibility (RESEARCH.md §Anti-Patterns). Add a comment: // CRITICAL: repeat wasm+topLevelAwait for Worker bundles — omitting this causes production builds to fail with "ESM integration proposal for Wasm is not supported".

    After both edits, run `npm run build` and verify it exits 0 with no TypeScript errors. If TypeScript reports exhaustiveness errors on LoadStage switch statements (e.g., in ProgressBar.tsx), do NOT fix those now — they are intentional signals that Plan 04's task must handle. Record the specific error locations in the task verify notes for the executor of Plan 04.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/reader/types.ts contains "no-imaging" in the LoadStage union
    - vite.config.ts contains `worker: { plugins: () => [wasm(), topLevelAwait()] }`
    - `npm run build` exits 0 (TypeScript exhaustiveness errors on ProgressBar/App for the new union member are expected — note them, do not suppress)
    - No `format: 'es'` in the worker block
  </acceptance_criteria>
  <done>LoadStage extended; vite.config.ts has worker.plugins factory; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Create src/worker/protocol.ts — typed postMessage boundary contract</name>
  <files>src/worker/protocol.ts</files>
  <read_first>
    - src/reader/types.ts — full file (all imported types for LoadResult and NonImagingResult)
    - src/reader/errors.ts — ReaderErrorClass, UnsupportedFinding shape
    - src/imaging/types.ts — ImagingGrid type (grid field in LoadResult)
    - .planning/phases/05-worker-offload-robustness-static-deploy/05-PATTERNS.md §protocol.ts — discriminated union patterns and file header convention
    - .planning/phases/05-worker-offload-robustness-static-deploy/05-RESEARCH.md §Pattern 2 — WorkerRequest and WorkerResponse shapes
  </read_first>
  <action>
    Create src/worker/protocol.ts. Follow the file-header convention from src/reader/types.ts: start with a block comment explaining the contract — "Typed postMessage protocol for the mzPeakWorker boundary. CONTRACT: only serializable/transferable types appear here. No Reader, no Arrow Table, no mzpeakts internals cross this boundary."

    Define WorkerRequest as a discriminated union with type discriminator:
      - { type: 'loadUrl'; url: string }
      - { type: 'loadFile'; bytes: ArrayBuffer; name: string }
      - { type: 'renderIonImage'; mz: number; tolDa: number; requestId: number }
      - { type: 'selectSpectrum'; index: number }

    Define WorkerResponse as a discriminated union:
      - { type: 'progress'; stage: LoadStage }
      - { type: 'loadResult'; result: LoadResult }
      - { type: 'noImaging'; result: NonImagingResult }
      - { type: 'renderResult'; ionImage: Float32Array | null; stats: IonImageStats | null; requestId: number }
      - { type: 'spectrumResult'; spectrum: SpectrumArrays }
      - { type: 'error'; class: ReaderErrorClass; message: string; findings?: UnsupportedFinding[] }

    Define IonImageStats as: { nonzeroCount: number; min: number; max: number } (mirrors the shape already in store.ts ionImageStats field).

    Define LoadResult: plain serializable object containing all fields the main thread needs after a successful imaging file load — manifest: ManifestEntry[], fileMeta: FileMeta, stats: FileStats, capabilities: Capabilities, grid: ImagingGrid | null, tic: Float32Array | null, mixedRepresentationWarning: string | null. NOTE: grid is ImagingGrid which may contain non-serializable internals — read src/imaging/types.ts to confirm ImagingGrid is a plain object before including it in LoadResult. If ImagingGrid contains methods or Maps that don't survive structured clone, use a plain serializable subset and note it.

    Define NonImagingResult: manifest: ManifestEntry[], fileMeta: FileMeta, stats: FileStats, capabilities: Capabilities. (No grid, no tic — per D-05.)

    All imports use relative paths. Export all types. No runtime code in this file — pure type declarations only.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npm run build 2>&1 | grep -i "protocol\|error" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/worker/protocol.ts exists with WorkerRequest, WorkerResponse, LoadResult, NonImagingResult, IonImageStats exported
    - All types are pure serializable/transferable — no Reader, Arrow Table, or mzpeakts internal types
    - File contains only type/interface exports, no runtime code
    - `npm run build` does not produce new errors related to protocol.ts
    - ImagingGrid inclusion decision is explicitly noted in a JSDoc comment if it has limitations
  </acceptance_criteria>
  <done>protocol.ts defines the complete typed message contract for the Worker boundary</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main thread → Worker | postMessage with WorkerRequest — only string/number/ArrayBuffer crosses |
| Worker → Main thread | postMessage with WorkerResponse — only plain objects and Float32Array buffers cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | WorkerRequest protocol | mitigate | Protocol types enforce only expected fields; no dynamic property access; union exhaustiveness checked by TypeScript |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase; audit table in RESEARCH.md confirms no new deps |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` exits 0
2. `grep "no-imaging" src/reader/types.ts` returns the new union member
3. `grep "worker.plugins" vite.config.ts` returns the factory line
4. `ls src/worker/protocol.ts` exists
5. `grep "WorkerRequest\|WorkerResponse\|LoadResult\|NonImagingResult" src/worker/protocol.ts` returns all four exports
6. TypeScript exhaustiveness errors on ProgressBar/App for 'no-imaging' are documented for Plan 04
</verification>

<success_criteria>
- LoadStage union includes 'no-imaging' at the correct position (before 'error')
- vite.config.ts worker.plugins uses the factory function form `() => [wasm(), topLevelAwait()]` with no format: 'es'
- protocol.ts is the single source of truth for all types crossing the Worker boundary
- npm run build exits 0 with no regressions
</success_criteria>

<output>
Create `.planning/phases/05-worker-offload-robustness-static-deploy/05-01-SUMMARY.md` when done.
</output>
