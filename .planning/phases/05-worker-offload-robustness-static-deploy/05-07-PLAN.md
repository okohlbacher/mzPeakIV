---
phase: 05-worker-offload-robustness-static-deploy
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/store.ts
autonomous: true
gap_closure: true
requirements:
  - UX-01

must_haves:
  truths:
    - "currentRequestId is reset to Date.now() at the start of both openUrl and openFile, before any await or state mutation"
    - "A stale renderResult from a previous file cannot pass the requestId guard after a new file is opened"
    - "worker.onerror and worker.onmessageerror are wired immediately after worker.onmessage"
    - "A Worker script crash surfaces as stage='error' with class='corrupt' instead of a permanent loading spinner"
    - "file.arrayBuffer() rejection is caught in openFile and routes to stage='error' instead of an unhandled rejection"
  artifacts:
    - path: "src/state/store.ts"
      provides: "currentRequestId reset in openUrl + openFile; worker.onerror + worker.onmessageerror handlers; try/catch around file.arrayBuffer()"
      contains: "currentRequestId = Date.now()"
  key_links:
    - from: "src/state/store.ts openUrl / openFile"
      to: "currentRequestId (module scope)"
      via: "assignment at top of action body"
      pattern: "currentRequestId = Date\\.now\\(\\)"
    - from: "src/state/store.ts worker.onerror"
      to: "useStore.setState stage='error'"
      via: "direct setState call"
      pattern: "worker\\.onerror"
    - from: "src/state/store.ts openFile"
      to: "file.arrayBuffer()"
      via: "try/catch block"
      pattern: "try.*arrayBuffer.*catch"
---

<objective>
Fix CR-02, CR-03, and CR-04 in src/state/store.ts: reset currentRequestId on new file open, add worker.onerror and worker.onmessageerror handlers, and wrap file.arrayBuffer() in a try/catch.

Purpose: Three wiring gaps in the store's Worker integration leave the UI in permanently-stuck loading states and allow stale ion images from a previous file to appear for the current file. All three fixes are in the same file with no inter-dependency.

Output: src/state/store.ts with all three gaps closed.
</objective>

<execution_context>
@/Users/kohlbach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohlbach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-02, CR-03, CR-04 in store.ts</name>

  <read_first>
    src/state/store.ts lines 96-153 (module-scope let, openUrl, openFile, renderIonImage actions)
    src/state/store.ts lines 155-241 (worker.onmessage handler block)
  </read_first>

  <files>src/state/store.ts</files>

  <action>
    Apply three targeted changes to src/state/store.ts.

    --- CR-02 fix: reset currentRequestId in openUrl and openFile ---

    In the openUrl action body (currently lines 104-107), add currentRequestId = Date.now(); as the first statement before the set call:

      openUrl(url: string) {
        currentRequestId = Date.now();
        set({ ...initialState, stage: "zip-index" });
        worker.postMessage({ type: "loadUrl", url } satisfies WorkerRequest);
      },

    In the openFile action body (currently lines 109-119), add currentRequestId = Date.now(); as the first statement before the set call. The reset MUST be before the await so any in-flight render from the previous file is invalidated immediately:

      async openFile(file: File) {
        currentRequestId = Date.now();
        set({ ...initialState, stage: "zip-index" });
        ...
      },

    Using Date.now() instead of 0 per WR-02 from REVIEW.md: avoids the edge case where a stale response with requestId=1 from file A matches requestId=1 for a render of file B when currentRequestId was reset to 0.

    --- CR-04 fix: try/catch around file.arrayBuffer() in openFile ---

    In openFile, wrap the await file.arrayBuffer() call in a try/catch. On catch, set stage='error' and return. The complete new openFile body:

      async openFile(file: File) {
        currentRequestId = Date.now();
        set({ ...initialState, stage: "zip-index" });
        let buffer: ArrayBuffer;
        try {
          buffer = await file.arrayBuffer();
        } catch (err) {
          set({
            stage: "error",
            error: {
              class: "corrupt",
              message: `Could not read file: ${err instanceof Error ? err.message : String(err)}`,
            },
          });
          return;
        }
        // Transfer ownership of the ArrayBuffer to the Worker (Pattern 4 / Pitfall 3).
        // File objects cannot cross the Worker boundary reliably — convert to ArrayBuffer
        // on the main thread first, then transfer the buffer zero-copy.
        worker.postMessage(
          { type: "loadFile", bytes: buffer, name: file.name } satisfies WorkerRequest,
          [buffer],
        );
      },

    --- CR-03 fix: add worker.onerror and worker.onmessageerror handlers ---

    Immediately after the closing brace of the worker.onmessage handler (after line 241, which is the closing `};`), add both handlers:

      worker.onerror = (e: ErrorEvent): void => {
        console.error("[mzPeakWorker] uncaught error:", e.message, e);
        useStore.setState({
          stage: "error",
          error: { class: "corrupt", message: `Worker error: ${e.message}` },
          isRendering: false,
        });
      };

      worker.onmessageerror = (e: MessageEvent): void => {
        console.error("[mzPeakWorker] message deserialization error:", e);
        useStore.setState({
          stage: "error",
          error: { class: "corrupt", message: "Worker message could not be deserialized." },
          isRendering: false,
        });
      };

    These handlers must follow the same pattern as the existing "error" case in onmessage (lines 227-239): set stage='error', set error.class='corrupt', clear isRendering.
  </action>

  <verify>
    <automated>grep -n "currentRequestId = Date.now()" /Users/kohlbach/Claude/mzPeakIV/src/state/store.ts</automated>
    <automated>grep -n "worker\.onerror" /Users/kohlbach/Claude/mzPeakIV/src/state/store.ts</automated>
    <automated>grep -n "worker\.onmessageerror" /Users/kohlbach/Claude/mzPeakIV/src/state/store.ts</automated>
    <automated>grep -n "arrayBuffer" /Users/kohlbach/Claude/mzPeakIV/src/state/store.ts</automated>
    <automated>grep -c "try" /Users/kohlbach/Claude/mzPeakIV/src/state/store.ts | xargs -I{} test {} -ge 1 && echo "PASS: try/catch present" || echo "FAIL: no try block found"</automated>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx tsc -b 2>&1 | tail -5; echo "TSC EXIT: $?"</automated>
  </verify>

  <done>
    grep "currentRequestId = Date.now()" src/state/store.ts shows two hits (one in openUrl, one in openFile).
    grep "worker.onerror" src/state/store.ts shows exactly one hit after the worker.onmessage block.
    grep "worker.onmessageerror" src/state/store.ts shows exactly one hit.
    grep "arrayBuffer" src/state/store.ts shows the await is inside a try/catch block.
    npx tsc -b exits 0.
    npm run build exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → main thread (onerror) | ErrorEvent.message from the Worker runtime — may contain internal WASM paths |
| main thread → store (openFile) | File.arrayBuffer() rejection message from the OS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | worker.onerror error message | accept | e.message from the Worker runtime is a browser-controlled string. No PII in WASM stack traces. Displayed to the user is acceptable — helps format implementers debug files. |
| T-05-07 | Denial of Service | file.arrayBuffer() unhandled rejection | mitigate | try/catch routes to stage='error' so the UI recovers; user can open a different file. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — pure code edits only. |
</threat_model>

<verification>
npm run build exits 0.
npx tsc -b exits 0.
grep "currentRequestId = Date.now()" src/state/store.ts produces 2 lines (openUrl, openFile).
grep "worker.onerror" src/state/store.ts produces 1 line.
grep "worker.onmessageerror" src/state/store.ts produces 1 line.
The try/catch wraps the arrayBuffer() call (verify with grep -A8 "openFile" src/state/store.ts).
</verification>

<success_criteria>
CR-02 closed: currentRequestId is reset to Date.now() in openUrl and openFile before any state mutation — stale renders from a previous file cannot pass the requestId guard.
CR-03 closed: worker.onerror and worker.onmessageerror set stage='error' and clear isRendering — Worker crashes surface as actionable errors rather than a permanent loading spinner.
CR-04 closed: file.arrayBuffer() rejection is caught in openFile and routes to stage='error' — OS read failures communicate to the user instead of leaving the UI stuck.
</success_criteria>

<output>
Create `.planning/phases/05-worker-offload-robustness-static-deploy/05-07-SUMMARY.md` when done.
</output>
