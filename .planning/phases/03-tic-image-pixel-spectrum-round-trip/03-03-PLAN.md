---
phase: 03-tic-image-pixel-spectrum-round-trip
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/state/store.ts
  - src/ui/ImagingPanel.tsx
  - src/ui/App.tsx
  - src/ui/SpectrumPanel.tsx
autonomous: true
requirements: [IMAGE-01, IMAGE-04, SPEC-01, SPEC-02]
must_haves:
  truths:
    - "Loading an imaging file renders a TIC heatmap on a Canvas immediately when the layout appears (new 'tic' stage)"
    - "Hovering the TIC canvas shows a 1-based x/y + TIC readout below it; '— no data' over absent pixels; clears on mouse leave"
    - "Clicking a present pixel selects that pixel's spectrum in the existing uPlot SpectrumPanel in-place"
    - "Clicking an absent pixel is a no-op (no selection change)"
    - "The selected pixel shows a 1px contrast ring drawn on the canvas, repainted on selection change"
    - "A non-imaging file renders the SpectrumPanel only — no empty canvas, no error"
    - "A mixed profile/centroid file surfaces a named warning; TIC uses the majority source"
  artifacts:
    - path: "src/ui/ImagingPanel.tsx"
      provides: "Canvas 2D TIC heatmap + hover readout + pixel-click hit-test + selection ring"
      exports: ["ImagingPanel"]
      contains: "tic-canvas"
    - path: "src/state/store.ts"
      provides: "tic slice + 'tic' LoadStage block in runLoad + useProfile derivation + mixed-representation flag"
      contains: "buildTic"
    - path: "src/ui/App.tsx"
      provides: "ImagingPanel rendered when grid !== null; 'tic' added to loading + stage sentinel"
    - path: "src/ui/SpectrumPanel.tsx"
      provides: "pixel-aware heading + optional mzWindow prop (SPEC-02 architectural placeholder)"
  key_links:
    - from: "src/state/store.ts runLoad"
      to: "src/compute/tic.ts buildTic + reader.extractXIC"
      via: "'tic' stage after 'grid'"
      pattern: "extractXIC\\(null, null"
    - from: "src/ui/ImagingPanel.tsx"
      to: "src/ui/rasterize.ts rasterizeTic + grid.coordToSpectrumIndex"
      via: "putImageData + hit-test -> selectSpectrum"
      pattern: "coordToSpectrumIndex"
    - from: "src/ui/App.tsx"
      to: "src/ui/ImagingPanel.tsx"
      via: "grid !== null conditional render"
      pattern: "ImagingPanel"
---

<objective>
Deliver the visible Core-Value round-trip slice: insert the eager `'tic'` LoadStage that computes the TIC raster via `extractXIC(null, null, useProfile)` + `buildTic`, build the `ImagingPanel` Canvas component that paints it (via `rasterizeTic` + `putImageData`), wire hover readout, pixel-click hit-test → `selectSpectrum`, and a selection ring, then mount it in `App.tsx` alongside the existing `SpectrumPanel`. After this plan, loading an imaging file shows a TIC image and clicking a pixel shows that pixel's spectrum.

Purpose: This is the end-to-end vertical slice (IMAGE-01 TIC, IMAGE-04 hover/orientation/aspect, SPEC-01 click→spectrum). It consumes the tested foundations from 03-01 (`buildTic`, `rasterizeTic`, `'tic'` LoadStage) and the representation routing from 03-02 (`selectSpectrum`).
Output: the `'tic'` store stage, `ImagingPanel.tsx`, and the App/SpectrumPanel wiring.

SPEC-02 reconciliation (RESEARCH A4 / orchestrator note): SPEC-02 (m/z window marker on the spectrum) has NO m/z-selection source in Phase 3 — CONTEXT and RESEARCH both defer the marker UI to Phase 4. This plan satisfies SPEC-02 *architecturally* (not visually): `SpectrumPanel` gains an optional `mzWindow?: { center: number; lo: number; hi: number } | null` prop defaulting to `null`, threaded as a no-op placeholder so Phase 4 can wire the marker without refactoring the component's signature. No marker is drawn in Phase 3 (there is nothing to mark). This is explicit scaffolding, NOT scope reduction — the Phase 3 deliverable is the round-trip; the marker is genuinely Phase-4-gated by the absence of m/z state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CONTEXT.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-RESEARCH.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-PATTERNS.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-UI-SPEC.md
@.planning/research/IMAGING-SPEC-ALIGNMENT.md

@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-01-SUMMARY.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-02-SUMMARY.md

<interfaces>
<!-- Contracts from plans 03-01 and 03-02 plus existing code. Build against these directly. -->

From src/compute/tic.ts (plan 03-01):
```
export function buildTic(xic: XIC, grid: ImagingGrid): Float32Array;  // length width*height
```

From src/ui/rasterize.ts (plan 03-01):
```
export function rasterizeTic(tic: Float32Array, grid: ImagingGrid): Uint8ClampedArray; // RGBA, length w*h*4
export function viridis(norm: number): [number, number, number];
```

From src/imaging/types.ts (Phase 2):
```
interface ImagingGrid {
  width; height; coordinateBase;
  pixelSizeUm: { x: number; y: number } | null;   // IMS:1000046/47 aspect (C5)
  coordToSpectrumIndex: Map<number, number>;       // key = y0*width + x0
  presenceMask: Uint8Array;                        // 1 = filled, 0 = absent
}
```

From src/reader/types.ts:
```
type LoadStage = "idle"|"zip-index"|"manifest"|"metadata"|"grid"|"tic"|"ready"|"error";  // "tic" added in 03-01
FileStats.representationCounts: { profile: number; centroid: number };
```

From vendor/mzpeakts/lib/src/reader.ts:
```
extractXIC(timeRange: Span1D|null, mzRange: Span1D|null, useProfile?: boolean): Promise<XIC|null>;
// extractXIC(null, null, useProfile) returns one XICPoint per spectrum with its FULL intensity array.
```

From src/state/store.ts (current shapes to extend):
```
type State = { reader; fileMeta; manifest; stats; capabilities; grid: ImagingGrid|null; stage: LoadStage; error; selectedIndex: number|null; selectedSpectrum; }
// runLoad: after the 'grid' block, before the final set({...stage:"ready"}). 'grid' block pattern at lines ~108-141.
// yieldFrame = () => new Promise(r => setTimeout(r, 0));
// selectSpectrum(index) already routes by representation (plan 03-02) — reuse unchanged.
```

From src/ui/App.tsx:
```
// loading = stage==="zip-index"||"manifest"||"metadata"||"grid"  (ADD "tic")
// hidden data-testid="stage" sentinel ternary (ADD a "tic" -> "Building TIC image…" arm)
// stage==="ready" <main>: left 400px <aside>, right <SpectrumPanel/>  (ADD <ImagingPanel/> when grid!==null)
```

From src/ui/SpectrumPanel.tsx:
```
// imperative uPlot via useRef; reads selectedIndex/selectedSpectrum/selectSpectrum from store.
// <h2>Spectrum</h2> heading; keep the numeric index input (secondary UX).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the eager 'tic' LoadStage + tic slice + useProfile derivation in store</name>
  <files>src/state/store.ts</files>
  <read_first>
    - src/state/store.ts (full file — the State type ~57-68, initialState ~76-87, the 'grid' block ~108-141, the final ready set ~143-154, imports)
    - src/compute/tic.ts (buildTic signature from 03-01)
    - src/reader/types.ts (FileStats.representationCounts; LoadStage with "tic")
    - vendor/mzpeakts/lib/src/reader.ts lines 252-296 (extractXIC signature + null-range behavior)
    - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CONTEXT.md (D-02 stage, D-08 useProfile derivation + mixed warning)
  </read_first>
  <action>
    In `src/state/store.ts`: add `import { buildTic } from "../compute/tic"`. Add `tic: Float32Array | null;` to the `State` type directly below `grid`, and `mixedRepresentationWarning: string | null;` (the D-08 named warning surface). Add `tic: null` and `mixedRepresentationWarning: null` to `initialState`. In `runLoad`, AFTER the existing `'grid'` block and BEFORE the final `set({ ...stage: "ready" })`, insert the `'tic'` block: only when `grid` is non-null, `set({ stage: "tic" })`, `await yieldFrame()`, derive `const { profile, centroid } = stats.representationCounts`, `const useProfile = centroid > 0 && profile === 0 ? false : true` (profile-default / majority-profile per D-08), `const mixed = profile > 0 && centroid > 0`, build the warning string when mixed (e.g. `Mixed profile/centroid spectra — TIC computed from ${useProfile ? "profile" : "centroid"} (${useProfile ? profile : centroid} of ${profile + centroid}); per-pixel spectra route individually`) else null, call `const xic = await reader.extractXIC(null, null, useProfile)`, and `const tic = xic ? buildTic(xic, grid) : null`. Add `tic` and `mixedRepresentationWarning` to the final ready `set({...})` payload. Wrap the extractXIC/buildTic call so a throw routes through `classifyError` into `set({ stage: "error", error: ... })` and returns (do not let a TIC failure crash the whole load silently — but a `null` xic is NOT an error, it yields `tic: null` which the UI renders as "Could not compute TIC for this file"). Keep the non-imaging path (`grid === null`) skipping TIC entirely → `tic: null`, straight to ready, no error (D-06).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx tsc --noEmit && npx vitest run src/state src/compute</automated>
  </verify>
  <acceptance_criteria>
    - `src/state/store.ts` State type contains `tic: Float32Array | null` and `mixedRepresentationWarning: string | null`
    - `runLoad` contains a `set({ stage: "tic" })` entry guarded by `if (grid)` and calls `reader.extractXIC(null, null, useProfile)` then `buildTic` (grep `buildTic` and `extractXIC`)
    - `useProfile` derivation reads `stats.representationCounts` (grep `representationCounts`)
    - The mixed-representation warning is set only when `profile > 0 && centroid > 0`
    - `npx tsc --noEmit` exits 0; `npx vitest run src/state src/compute` exits 0
  </acceptance_criteria>
  <done>The store gains a tic slice and an eager 'tic' stage that computes the raster via the same extractXIC primitive Phase 4 will reuse; mixed files set a named warning; non-imaging files skip TIC cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: ImagingPanel Canvas component — paint TIC, hover readout, click hit-test, selection ring</name>
  <files>src/ui/ImagingPanel.tsx</files>
  <read_first>
    - src/ui/SpectrumPanel.tsx (the imperative useRef-mount + store-selector skeleton + data-testid/aria-label/inline-style conventions to mirror)
    - src/ui/rasterize.ts (rasterizeTic, viridis — from 03-01)
    - src/imaging/types.ts (ImagingGrid: width/height/coordToSpectrumIndex/presenceMask/pixelSizeUm/coordinateBase)
    - src/state/store.ts (the tic slice + selectSpectrum + selectedIndex from Task 1 / plan 03-02)
    - src/ui/GridDiagnosticsPanel.tsx (collapsible warning pattern + WARNING="#8a6d00" amber for the mixed-representation surface)
    - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-UI-SPEC.md (Canvas data colors; sentinel; ring contrast white/black; hover-readout copy; cursor:crosshair; aspect via pixelSizeUm; Interaction Contract table)
    - .planning/research/IMAGING-SPEC-ALIGNMENT.md (C2 fixed orientation no-flip; C3 1-based display; C5 aspect; C8 sparse)
  </read_first>
  <action>
    Create `src/ui/ImagingPanel.tsx` mirroring the `SpectrumPanel.tsx` React skeleton (imperative canvas via `useRef<HTMLCanvasElement>(null)`, store selectors). Select `grid`, `tic`, `selectedIndex`, `selectSpectrum`, and `mixedRepresentationWarning` from the store. Render `<section aria-label="imaging-panel" data-testid="imaging-panel">` containing an `<h2>TIC Image</h2>`, a `<canvas data-testid="tic-canvas">`, a hover-readout `<div data-testid="tic-hover-readout">` below the canvas, and (when `mixedRepresentationWarning` is set) an amber warning line (`#8a6d00`, `⚠` glyph) following the GridDiagnosticsPanel pattern. When `tic === null` but `grid !== null` show the muted text `Could not compute TIC for this file`.

    Paint pass (useEffect keyed on `[tic, grid]`): set the canvas intrinsic size to `grid.width × grid.height`, get `ctx`, call `rasterizeTic(tic, grid)` → wrap in `new ImageData(rgba, grid.width, grid.height)` → `ctx.putImageData(img, 0, 0)`. Set the canvas CSS display size to honor pixel aspect from `grid.pixelSizeUm` (x:y; default 1:1 when null) and cap the displayed width to the pane — NEVER stretch to square. Use `image-rendering: pixelated` so pixels stay crisp when CSS-scaled. Set `cursor: crosshair` on the canvas.

    Selection-ring pass (2nd useEffect keyed on `[selectedIndex, tic, grid]`, runs AFTER the paint): re-run the paint (putImageData overwrites composite), then if `selectedIndex` maps to a grid cell, derive `(x0, y0)` for that selected spectrum (invert via a lookup: iterate `grid.coordToSpectrumIndex` once to find the key whose value === selectedIndex, then `x0 = key % grid.width`, `y0 = Math.floor(key / grid.width)`), pick ring color by the selected pixel's luminance (white `#fff` default, black `#000` if the pixel is light), and `ctx.strokeStyle = ringColor; ctx.lineWidth = 1; ctx.strokeRect(x0 + 0.5, y0 + 0.5, 1, 1)`.

    Hit-test helper `toGridCoord(e, canvas, grid)` (resolution/aspect-safe per Pitfall 5 — use the bounding rect, NOT offsetX): `const rect = canvas.getBoundingClientRect(); const x0 = Math.floor(((e.clientX - rect.left)/rect.width)*grid.width); const y0 = Math.floor(((e.clientY - rect.top)/rect.height)*grid.height);` return null when out of `[0,width)`/`[0,height)`, else `{ x0, y0, key: y0*grid.width + x0 }`. onMouseMove: compute the hit; when present (`grid.presenceMask[key] !== 0`) set the readout `useState` string to `x: ${x0 + grid.coordinateBase}, y: ${y0 + grid.coordinateBase} · TIC: ${formatCompact(tic[key])}` (1-based display via coordinateBase — C3; compact format like `1.4e6`), when absent set `x: ${x0+base}, y: ${y0+base} — no data` (muted), when off-canvas clear it. onMouseLeave: clear the readout (empty string — D-05). onClick: compute the hit; guard `grid.presenceMask[hit.key] !== 0` (absent click is a no-op — D-04); `const idx = grid.coordToSpectrumIndex.get(hit.key); if (idx != null) void selectSpectrum(idx)`. Do NOT introduce any flip/transpose — reuse the grid key formula exactly (C2 MANDATORY).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx tsc --noEmit && npx eslint src/ui/ImagingPanel.tsx</automated>
    <human-check>Load an imaging .mzpeak (or PXD001283 if present in test/data/): a TIC heatmap appears immediately; absent pixels are visibly distinct (near-black #1a1a1a) from low-intensity colormap pixels; hovering shows 1-based x/y + TIC, "— no data" over gaps, and clears on leave; clicking a present pixel updates the spectrum panel below and draws a 1px contrast ring on the clicked pixel; clicking a gap does nothing. Verified at phase end (human_verify_mode=end-of-phase).</human-check>
  </verify>
  <acceptance_criteria>
    - `src/ui/ImagingPanel.tsx` exports `ImagingPanel` and contains `data-testid="tic-canvas"` and `data-testid="tic-hover-readout"`
    - The component calls `rasterizeTic` + `putImageData` and draws the ring with `strokeRect` in a second pass (grep `putImageData` and `strokeRect`)
    - Hit-test uses `getBoundingClientRect` (NOT `offsetX`) — grep `getBoundingClientRect`, and grep confirms NO `offsetX`
    - Hover readout adds `grid.coordinateBase` for 1-based display (grep `coordinateBase`)
    - Click guards `presenceMask` before calling `selectSpectrum` (grep `presenceMask` and `selectSpectrum`)
    - Canvas CSS honors `pixelSizeUm` aspect (grep `pixelSizeUm`); no transpose (grep confirms no swapped `x0`/`y0` in the key — key is `y0 * grid.width + x0`)
    - `npx tsc --noEmit` and `npx eslint src/ui/ImagingPanel.tsx` exit 0
  </acceptance_criteria>
  <done>ImagingPanel paints the TIC, reports 1-based hover coords with sparse "— no data", routes pixel-clicks to selectSpectrum with a no-op on gaps, draws a contrast selection ring, honors pixel aspect, and surfaces the mixed-representation warning — all with the fixed (non-flipped) orientation.</done>
</task>

<task type="auto">
  <name>Task 3: Mount ImagingPanel in App; pixel-aware SpectrumPanel heading + SPEC-02 mzWindow placeholder</name>
  <files>src/ui/App.tsx, src/ui/SpectrumPanel.tsx</files>
  <read_first>
    - src/ui/App.tsx (loading derivation ~15-19, hidden stage sentinel ~40-53, the ready <main> block ~60-82)
    - src/ui/ImagingPanel.tsx (the component from Task 2)
    - src/ui/SpectrumPanel.tsx (full file — heading ~85, index input ~86-109, the selectors)
    - src/state/store.ts (grid selector; selectedIndex; grid.coordToSpectrumIndex for pixel-coord display)
    - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-UI-SPEC.md (Copywriting + Layout Contract: imaging file stacks ImagingPanel over SpectrumPanel; non-imaging = SpectrumPanel only; heading "Spectrum — pixel (x1, y1)" in imaging mode)
  </read_first>
  <action>
    In `src/ui/App.tsx`: import `ImagingPanel`. Add `stage === "tic"` to the `loading` boolean. Add a `"tic"` arm to the hidden `data-testid="stage"` sentinel ternary rendering `"Building TIC image…"` (mirror the existing `"grid"` → `"Building imaging grid…"` arm). In the `stage === "ready"` `<main>`, select `const grid = useStore((s) => s.grid)`; when `grid !== null` render the right pane as a vertical stack `<div style={{display:"flex",flexDirection:"column",flex:1,minWidth:0}}>` containing `<ImagingPanel />` (top) then `<SpectrumPanel />` (below); when `grid === null` keep the current bare `<SpectrumPanel />` unchanged (no empty canvas — D-06 / UI-SPEC layout contract).

    In `src/ui/SpectrumPanel.tsx`: add an optional prop `mzWindow?: { center: number; lo: number; hi: number } | null` defaulting to `null` — this is the SPEC-02 architectural placeholder (no marker drawn in Phase 3; Phase 4 wires it). Add a comment citing SPEC-02 deferral. Select `grid` and `selectedIndex` from the store; when `grid !== null` and a pixel is selected, derive the 1-based `(x1, y1)` by finding the grid key whose `coordToSpectrumIndex` value === `selectedIndex` (`x1 = key % grid.width + grid.coordinateBase`, `y1 = Math.floor(key / grid.width) + grid.coordinateBase`) and render the `<h2>` as `Spectrum — pixel (${x1}, ${y1})`; otherwise keep the plain `<h2>Spectrum</h2>`. Keep the numeric index input fully intact (secondary UX — D-04). Do NOT draw any m/z marker (no m/z state exists yet — SPEC-02 marker is Phase 4).
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx tsc --noEmit && npx eslint src/ui/App.tsx src/ui/SpectrumPanel.tsx && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/App.tsx` imports and conditionally renders `ImagingPanel` only when `grid !== null` (grep `ImagingPanel` and `grid !==` / `grid &&`)
    - `src/ui/App.tsx` `loading` includes `stage === "tic"` and the stage sentinel has a `"tic"` arm with `"Building TIC image"`
    - `src/ui/SpectrumPanel.tsx` declares an optional `mzWindow` prop defaulting to `null` with a SPEC-02 deferral comment (grep `mzWindow` and `SPEC-02`)
    - `src/ui/SpectrumPanel.tsx` heading reads `Spectrum — pixel (` form when a pixel is selected in imaging mode (grep `pixel (`); numeric index input retained (grep `data-testid="spectrum-index"`)
    - `npx tsc --noEmit`, `npx eslint src/ui/App.tsx src/ui/SpectrumPanel.tsx`, and `npx vitest run` all exit 0
  </acceptance_criteria>
  <done>App stacks ImagingPanel over SpectrumPanel for imaging files (SpectrumPanel-only for non-imaging), the spectrum heading reflects the clicked pixel, the SPEC-02 mzWindow placeholder is threaded as a no-op, and the full unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer event → grid coord | User mouse position mapped to a grid cell index used to index typed arrays and a Map. |
| TIC `Float32Array` → ImageData | Computed bytes painted to a canvas; intrinsic size = already-capped grid dimensions. |
| representationCounts → useProfile | File-derived counts select which signal source the whole-file TIC reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering / DoS | hit-test indexing | mitigate | `toGridCoord` returns null when outside `[0,width)`/`[0,height)`; clicks/hover never index a typed array OOB. |
| T-03-08 | Spoofing (false data) | absent vs zero pixel | mitigate | Presence-mask sentinel (#1a1a1a, plan 03-01) keeps "missing acquisition" visually distinct from "real zero" (C8); absent clicks are no-ops. |
| T-03-09 | Repudiation (silent wrong source) | mixed-representation TIC | mitigate | D-08 named warning surfaced in ImagingPanel when both profile and centroid spectra exist; per-pixel reads still route per-spectrum (plan 03-02). |
| T-03-10 | DoS | TIC compute on large grids (PXD001283 ~34,840 px main-thread) | accept | Single synchronous pass on an already-capped grid; worker offload is explicitly Phase 5. Acceptable for orientation-tool scale. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (RESEARCH: zero dependencies). slopcheck N/A. |
</threat_model>

<verification>
- `npx vitest run` — full unit suite (compute, rasterize, reader routing, store) green.
- `npx tsc --noEmit` + `npx eslint` on the three UI files — clean.
- `<human-check>` at phase end: imaging file → TIC visible → hover readout → click → spectrum + ring → absent-click no-op; non-imaging file → SpectrumPanel only.
</verification>

<success_criteria>
- IMAGE-01: TIC renders as the default spatial overview the moment the layout appears.
- IMAGE-04: hover shows 1-based x/y + TIC; fixed orientation (no flip); pixel aspect honored; sparse ≠ zero.
- SPEC-01: clicking a present pixel shows that pixel's spectrum in uPlot in-place; ring marks the selection.
- SPEC-02: architecturally enabled via the `mzWindow` placeholder prop (no marker drawn — Phase 4 gated).
- Non-imaging files render SpectrumPanel only; mixed files warn.
</success_criteria>

<output>
Create `.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-03-SUMMARY.md` when done.
</output>
