---
phase: 02-imaging-grid-reconstruction-the-gate
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND1.log
  - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND2.log
autonomous: false
requirements: [IMG-01, IMG-02, IMG-03]
must_haves:
  truths:
    - "Codex round1 (plan bundle) and round2 (phase diff) have been run for Phase 2"
    - "Each verdict line (accept / accept-with-revisions / reject) is captured for the phase commit footer"
    - "All Phase-2 success criteria are demonstrably met by the full test suite + build"
    - "The reader import boundary still holds and no bigint/Arrow types leak into src/imaging/"
  artifacts:
    - path: ".planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND1.log"
      provides: "Adversarial review of the Phase-2 plan bundle"
    - path: ".planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND2.log"
      provides: "Adversarial review of the Phase-2 diff"
  key_links:
    - from: "tools/codex_review.sh"
      to: ".planning/phases/02-imaging-grid-reconstruction-the-gate/"
      via: "round1/round2 write logs here"
      pattern: "02-CODEX-ROUND"
---

<objective>
Final gate for Phase 2 (PROC-01): run the Codex adversarial review harness against this phase's plan bundle (round1) and the phase diff (round2), capture the verdicts, run the full verification suite, re-assert the reader/imaging boundaries, and surface any non-accept verdict to the operator for adjudication.

Purpose: Per CLAUDE.md PROC-01, every phase is bracketed by a Codex CLI adversarial review. This plan exercises `tools/codex_review.sh` and gates the phase on a green build + reviewed verdicts. Claude runs the CLI; the operator decides what to do with non-accept verdicts.
Output: Two Codex log files + verdict lines for the phase commit footer; a confirmed-green Phase-2 deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-CONTEXT.md

<depends_on_summary>
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-01-SUMMARY.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-02-SUMMARY.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-03-SUMMARY.md
</depends_on_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase-2 verification sweep + boundary re-assertion</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 Success Criteria #1-#5)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-01-SUMMARY.md, 02-02-SUMMARY.md, 02-03-SUMMARY.md (what each plan delivered + verify commands)
  </read_first>
  <action>
    Run the complete Phase-2 verification: `npm run lint`, `npx tsc --noEmit`, `npm test` (all Vitest suites incl. scanCoords, grid, store), `npm run build`, and `npx playwright test` (if any imaging fixture/e2e exists; otherwise note unit coverage is the gate per D-01). Confirm against ROADMAP Phase-2 success criteria: (1) coords reconstructed per spec via the swappable CoordSource chain that logs which strategy won and accepts Int64/UInt32; (2) grid geometry — declared-extent-wins, presence mask, coord↔spectrum lookup, 1-based→0-based via coordinate_base, pixel aspect; (3) diagnostics panel — dims, unique-coord vs spectrum count, fill ratio, missing/duplicate, discovery-vs-authoritative disagreement; (4) validation against synthetic known-grid fixtures (PXD001283 unlock test present + auto-skipping); (5) non-imaging file reported distinctly as "not imaging data," not a broken file. Confirm IMG-01/IMG-02/IMG-03 each map to ≥1 passing test. Re-assert the import boundary: `grep -rl "from ['\"]mzpeakts['\"]" src/` returns only files under `src/reader/`. Re-assert no `bigint`/Arrow leak above the reader: no file under `src/imaging/` imports from `src/reader/` or `mzpeakts`. Record any failure as a blocker (do not paper over) before invoking Codex.
  </action>
  <verify>
    <automated>npm run lint && npx tsc --noEmit && npm test && npm run build && bash -c 'test -z "$(grep -rl "from [\x27\"]mzpeakts[\x27\"]" src/ | grep -v "^src/reader/")" && test -z "$(grep -rl "from [\x27\"]\.\./reader" src/imaging/ 2>/dev/null)"'</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the verify line exits 0.
    - Each of the five ROADMAP Phase-2 success criteria maps to at least one passing test or build artifact (note the mapping in the SUMMARY).
    - `grep -rl "from ['\"]mzpeakts['\"]" src/` returns only files under `src/reader/` (boundary held).
    - No file under `src/imaging/` imports from `src/reader/` or `mzpeakts` (one-way boundary held).
    - IMG-01, IMG-02, IMG-03 each map to ≥1 named passing test (recorded in the SUMMARY).
  </acceptance_criteria>
  <done>Full suite green; all five Phase-2 success criteria demonstrably met; both boundaries re-asserted.</done>
</task>

<task type="auto">
  <name>Task 2: Run Codex round1 (plan bundle) and round2 (phase diff)</name>
  <files>.planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND1.log, .planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND2.log</files>
  <read_first>
    - CLAUDE.md (PROC-01: Claude runs tools/codex_review.sh round1/round2; copies verdict into the phase commit footer; only escalate on reject or substantive accept-with-revisions; codex CLI at /opt/homebrew/bin/codex; logs gitignored)
    - tools/codex_review.sh (the harness bootstrapped in Phase 1)
  </read_first>
  <action>
    Determine the phase-start SHA (the commit immediately before Phase-2 execution began — `3bf10d2313762275d68a643be87bd7cd1dacc808` was HEAD at planning time; confirm the actual phase-start commit and record it in the SUMMARY). Run `bash tools/codex_review.sh round1 02` (adversarial read of the Phase-2 plan bundle) and `bash tools/codex_review.sh round2 02 --sha <phase_start_sha>` (adversarial read of the phase diff). Capture the verdict line from each (`accept` / `accept-with-revisions` / `reject`) and record both in the SUMMARY for the phase commit footer. Logs land at `.planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND{1,2}.log` (gitignored).
  </action>
  <verify>
    <automated>test -f .planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND1.log && test -f .planning/phases/02-imaging-grid-reconstruction-the-gate/02-CODEX-ROUND2.log</automated>
  </verify>
  <acceptance_criteria>
    - Both round1 and round2 logs exist and contain a recognizable verdict line.
    - Both verdict lines are recorded in 02-04-SUMMARY.md for the commit footer.
  </acceptance_criteria>
  <done>Both Codex rounds run; verdicts captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 complete: reader-boundary CoordSource chain (Int64/UInt32, source_index join, geometry despite dropped ms_run.parameters), pure sparse ImagingGrid builder (declared-extent-wins, coordinate_base normalization, presence mask, diagnostics), eager 'grid' LoadStage, inline GridDiagnosticsPanel, and the distinct non-imaging notice — plus Codex review verdicts.</what-built>
  <how-to-verify>
    1. Review the two Codex verdict lines reported in 02-04-SUMMARY.md (round1 plan, round2 diff).
    2. If both are `accept`, approve to close the phase.
    3. If either is `reject` or a substantive `accept-with-revisions`, decide whether to address now (loop back via gap closure) or accept the noted risk.
    4. Optionally run `npm run preview`, open an imaging `.mzpeak` (when available) and confirm the Grid panel shows the compact line + expandable diagnostics; open the non-imaging `small.mzpeak` and confirm the muted "Not imaging data" notice (no error banner).
    5. Note: PXD001283 validation remains gated until the operator converts the file into `test/data/PXD001283.mzpeak`; until then the unlock test auto-skips and synthetic fixtures are the gate (D-01).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 2, or describe required revisions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex CLI review of plan/diff | Advisory adversarial review; operator adjudicates verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-REPUD | Repudiation | unrecorded review verdict | mitigate | Both verdict lines captured in the SUMMARY for the phase commit footer (PROC-01). |
| T-02-04-SC | Tampering | no new package installs | accept | Review/gate plan; adds no dependencies (whole phase installs zero packages). |
</threat_model>

<verification>
- Full suite green (Task 1).
- Both Codex logs present with verdict lines (Task 2).
- Operator approval recorded (Task 3 checkpoint).
</verification>

<success_criteria>
- Phase 2 is reviewed (PROC-01 round1 + round2), green across lint/types/tests/build, both boundaries re-asserted, and approved by the operator.
</success_criteria>

<output>
Create `.planning/phases/02-imaging-grid-reconstruction-the-gate/02-04-SUMMARY.md` when done, including both Codex verdict lines and the phase-start SHA.
</output>
