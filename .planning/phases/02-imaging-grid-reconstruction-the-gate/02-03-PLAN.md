---
phase: 02-imaging-grid-reconstruction-the-gate
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/reader/types.ts
  - src/state/store.ts
  - src/state/store.test.ts
  - src/ui/GridDiagnosticsPanel.tsx
  - src/ui/App.tsx
autonomous: true
requirements: [IMG-01, IMG-02, IMG-03]
must_haves:
  truths:
    - "A new 'grid' LoadStage runs between 'metadata' and 'ready', eager during file load, with visible staged progress"
    - "store.grid holds an ImagingGrid for imaging files and null for non-imaging files; non-imaging is NOT an error"
    - "The left panel shows a compact grid summary line below CapabilitiesPanel; clicking expands a diagnostics table"
    - "A non-imaging file shows the muted 'Not imaging data — no spatial coordinates found' notice, never the ErrorBanner"
    - "An imaging fixture loaded end-to-end produces a non-null grid with filledCount === unique coord count"
  artifacts:
    - path: "src/state/store.ts"
      provides: "'grid' stage block calling extractCoords + readGridGeometry + buildImagingGrid; grid: ImagingGrid | null state field"
      contains: "stage: \"grid\""
    - path: "src/ui/GridDiagnosticsPanel.tsx"
      provides: "Inline expandable grid diagnostics panel mirroring CapabilitiesPanel"
      exports: ["GridDiagnosticsPanel"]
    - path: "src/reader/types.ts"
      provides: "LoadStage union extended with 'grid'"
      contains: "\"grid\""
  key_links:
    - from: "src/state/store.ts"
      to: "src/reader/scanCoords.ts + src/imaging/grid.ts"
      via: "extractCoords/readGridGeometry/buildImagingGrid in the grid stage"
      pattern: "buildImagingGrid"
    - from: "src/ui/App.tsx"
      to: "src/ui/GridDiagnosticsPanel.tsx"
      via: "mounted below CapabilitiesPanel in the left aside"
      pattern: "GridDiagnosticsPanel"
    - from: "src/ui/GridDiagnosticsPanel.tsx"
      to: "store.grid"
      via: "useStore selector"
      pattern: "useStore"
---

<objective>
Complete the Phase-2 vertical slice: wire the 02-01 reader extractor and the 02-02 grid builder into the store as a new eager `'grid'` LoadStage, surface the result in the inline `GridDiagnosticsPanel` below CapabilitiesPanel, and report non-imaging files as a calm muted notice (not an error). After this plan, loading an imaging `.mzpeak` produces a visible, verified grid summary and an expandable diagnostics table — the user-facing Phase-2 capability.

Purpose: IMG-01/02/03 integration — turns the two pure layers into an observable end-to-end capability per D-03/D-04/D-05/D-06. This is the slice that makes the phase demonstrable.
Output: extended `LoadStage`, `store.grid`, the `'grid'` stage block, `GridDiagnosticsPanel`, App integration, and an end-to-end store/integration test proving the slice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-CONTEXT.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-RESEARCH.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-PATTERNS.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-UI-SPEC.md

<depends_on_summary>
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-01-SUMMARY.md
@.planning/phases/02-imaging-grid-reconstruction-the-gate/02-02-SUMMARY.md
</depends_on_summary>

<interfaces>
<!-- The store calls these three functions delivered by 02-01 and 02-02. -->
From src/reader/scanCoords.ts (02-01):
```typescript
export function extractCoords(reader: Reader): CoordResult | null;       // { coords, spectrumIndices, strategy }
export function readGridGeometry(reader: Reader): GridGeometry | null;
```
From src/imaging/grid.ts (02-02):
```typescript
export function buildImagingGrid(
  coords: { x: number; y: number }[],
  spectrumIndices: number[],
  geometry: GridGeometry | null,
  coordSourceStrategy: CoordResult["strategy"],
): ImagingGrid | null;
```
From src/imaging/types.ts (02-02): `ImagingGrid`, `GridDiagnostics`.

<!-- Current store grid-stage insertion point (src/state/store.ts runLoad, after the metadata block) -->
```typescript
set({ stage: "metadata" }); await yieldFrame();
const fileMeta = readFileMeta(reader);
const stats = computeStats(reader, manifest);
const capabilities = computeCapabilities(reader, manifest);
// → INSERT 'grid' stage block HERE, before the final set({ ..., stage: "ready" })
set({ reader, manifest, fileMeta, stats, capabilities, stage: "ready", ... });
```

<!-- App.tsx left aside (mount point) + loading flag + stage sentinel -->
```typescript
const loading = stage === "zip-index" || stage === "manifest" || stage === "metadata"; // add "grid"
<MetadataPanel /><StatsPanel /><CapabilitiesPanel /> // append <GridDiagnosticsPanel />
```
</interfaces>

<ui_contract>
<!-- BINDING from 02-UI-SPEC.md. The panel MUST reuse the inline-style pattern; introduce no CSS framework. -->
- Required data-testid hooks: `grid-panel`, `grid-summary-line`, `grid-expand-toggle`, `grid-diagnostics-table`, `grid-not-imaging-notice`, `grid-anomaly-warning`.
- Compact summary (healthy): `Grid: {width}×{height} — {filledCount}/{totalCells} px filled ({pct}%)` using `×` (U+00D7) and `toLocaleString()` for thousands grouping. Anomaly prefix `⚠ ` (U+26A0) + warning amber `#8a6d00` when `filledCount < totalCells*0.95` OR `duplicateCount > 0` OR a discovery disagreement exists.
- Non-imaging (grid === null AND capabilities.isImaging === false): single muted `#888` line `Not imaging data — no spatial coordinates found` (testid `grid-not-imaging-notice`). NO dimensions, NO expand, NO table, NEVER ErrorBanner.
- Expanded table rows (testids on the `<td>`): Dimensions `{w} × {h} px`; Fill `{filled} / {total} pixels ({pct}% filled)`; Spectra `{uniqueCoordCount} unique coords / {spectrumCount} spectra`; Missing `{missingCount} pixels`; Duplicates `{duplicateCount} pixels` (amber when >0); Pixel size `{x} × {y} µm` or `1:1 (assumed — no IMS:1000046/47)` muted when null; Coord source the winning strategy label; Discovery check `agrees` (muted) or the disagreement note (amber).
- Colors (BINDING, do NOT use error red `#b71c1c`): muted `#888`, success `#1b5e20`, warning amber `#8a6d00`. Section: `padding: "0.5rem"`, `borderTop: "1px solid #eee"`, `<h3 style={{ margin: "0 0 0.4rem" }}>Grid</h3>`, table `fontSize: "0.8rem"`. Render nothing (`return null`) until `capabilities` is set.
</ui_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LoadStage + store with the eager 'grid' stage and grid state</name>
  <files>src/reader/types.ts, src/state/store.ts, src/state/store.test.ts</files>
  <read_first>
    - src/reader/types.ts (LoadStage union, lines 76-82)
    - src/state/store.ts (State type, initialState, runLoad staged transitions)
    - src/state/store.test.ts (existing store-test shape to extend)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-PATTERNS.md (store extension pattern, lines 452-538)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-01-SUMMARY.md, 02-02-SUMMARY.md (exact exported signatures)
  </read_first>
  <action>
    In `src/reader/types.ts`, add `"grid"` to the `LoadStage` union between `"metadata"` and `"ready"`. In `src/state/store.ts`: import `extractCoords`, `readGridGeometry` from `../reader/scanCoords` and `buildImagingGrid` from `../imaging/grid` and `ImagingGrid` (type) from `../imaging/types`; add `grid: ImagingGrid | null` to the `State` type (after `capabilities`) and to `initialState` (`grid: null`). In `runLoad`, after the metadata computations and before the final `set({ ..., stage: "ready" })`, insert: `set({ stage: "grid" }); await yieldFrame();` then `let grid: ImagingGrid | null = null; if (capabilities.isImaging) { const cr = extractCoords(reader); const geometry = readGridGeometry(reader); grid = cr ? buildImagingGrid(cr.coords, cr.spectrumIndices, geometry, cr.strategy) : null; }` and include `grid` in the final `set(...)`. Use the existing `yieldFrame` — do NOT add a new one. Non-imaging files (capabilities.isImaging false) leave `grid: null` and proceed to `ready` with NO error (D-06). Extend `store.test.ts` with: (a) an imaging-path test (mock or fixture) asserting `stage` passes through `"grid"` and `grid` is non-null; (b) a non-imaging test (the `small.mzpeak` fixture, D-02) asserting `grid === null`, `stage === "ready"`, and `error === null`.
  </action>
  <verify>
    <automated>npx vitest run src/state/store.test.ts && npx tsc --noEmit && grep -q '"grid"' src/reader/types.ts && grep -q 'buildImagingGrid' src/state/store.ts</automated>
  </verify>
  <acceptance_criteria>
    - `LoadStage` contains `"grid"` between `"metadata"` and `"ready"`.
    - `store.ts` State + initialState carry `grid: ImagingGrid | null` / `grid: null`.
    - The `'grid'` stage block calls `extractCoords`, `readGridGeometry`, and `buildImagingGrid` only when `capabilities.isImaging`.
    - store.test.ts proves: imaging path → `grid` non-null and `stage` reached `"grid"`; non-imaging `small.mzpeak` → `grid === null`, `stage === "ready"`, `error === null`.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Eager 'grid' stage wired; store.grid populated for imaging, null (no error) for non-imaging; store tests green.</done>
</task>

<task type="auto">
  <name>Task 2: GridDiagnosticsPanel + App integration</name>
  <files>src/ui/GridDiagnosticsPanel.tsx, src/ui/App.tsx</files>
  <read_first>
    - src/ui/CapabilitiesPanel.tsx (exact panel pattern to clone — section, table, conditional spans)
    - src/ui/App.tsx (left aside mount point, loading flag, stage sentinel arms)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-UI-SPEC.md (binding copy + color + testid contract)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-PATTERNS.md (GridDiagnosticsPanel pattern, lines 324-420)
  </read_first>
  <action>
    Create `src/ui/GridDiagnosticsPanel.tsx` by cloning `CapabilitiesPanel.tsx`: `import { useState } from "react"` + `import { useStore } from "../state/store"`. Read `grid` and `capabilities` from the store; `if (!capabilities) return null`. If `grid === null` → render the muted non-imaging notice (`grid-not-imaging-notice`, `#888`, exact copy `Not imaging data — no spatial coordinates found`). Otherwise render the `grid-panel` `<section>` (`padding: "0.5rem"`, `borderTop: "1px solid #eee"`, `<h3 style={{ margin: "0 0 0.4rem" }}>Grid</h3>`) with the clickable compact summary line (`grid-summary-line` / `grid-expand-toggle`, `cursor: pointer`, `aria-expanded`, leading `▸`/`▾`) computing `pct = Math.round(filledCount/totalCells*100)` and the anomaly flag (`filledCount < totalCells*0.95 || diagnostics.duplicateCount > 0 || diagnostics.discoveryDisagreement != null`) → amber `#8a6d00` + `⚠ ` prefix + `grid-anomaly-warning`. On expand (`useState`), render the `grid-diagnostics-table` (`fontSize: "0.8rem"`, `borderCollapse: "collapse"`) with the exact rows from the UI contract (Dimensions, Fill, Spectra, Missing, Duplicates, Pixel size, Coord source, Discovery check). Use `toLocaleString()` on the px counts and `×` (U+00D7). Do NOT use error red. In `src/ui/App.tsx`: import and append `<GridDiagnosticsPanel />` immediately after `<CapabilitiesPanel />` in the left `<aside>`; add `"grid"` to the `loading` flag; add a `stage === "grid" ? "Building imaging grid…" :` arm to the hidden stage sentinel between the metadata and ready arms.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -q 'GridDiagnosticsPanel' src/ui/App.tsx && grep -q 'grid-not-imaging-notice' src/ui/GridDiagnosticsPanel.tsx && grep -q 'grid-summary-line' src/ui/GridDiagnosticsPanel.tsx && bash -c 'test -z "$(grep -n "#b71c1c" src/ui/GridDiagnosticsPanel.tsx)"'</automated>
  </verify>
  <acceptance_criteria>
    - `GridDiagnosticsPanel.tsx` exports `GridDiagnosticsPanel`, reads `grid`+`capabilities`, returns null until `capabilities` is set.
    - All six required testids present: `grid-panel`, `grid-summary-line`, `grid-expand-toggle`, `grid-diagnostics-table`, `grid-not-imaging-notice`, `grid-anomaly-warning`.
    - Non-imaging branch renders the exact muted copy and NO expand affordance / NO table.
    - The panel uses ONLY `#888`, `#1b5e20`, `#8a6d00` — `grep -n "#b71c1c"` returns empty (no error red).
    - `App.tsx` mounts `<GridDiagnosticsPanel />` after `<CapabilitiesPanel />`, includes `"grid"` in `loading`, and has the `"Building imaging grid…"` sentinel arm.
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>GridDiagnosticsPanel renders the compact line + expandable table for imaging files and the muted notice for non-imaging files, mounted below CapabilitiesPanel, with all UI-SPEC testids/colors honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store grid stage → UI | Already-validated in-memory grid POJO crosses to presentational React; no new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-MISCLASS | Spoofing | non-imaging file rendered as an error | mitigate | Non-imaging is `grid: null` + a muted notice in GridDiagnosticsPanel; store sets NO error and reaches `ready` (D-04/D-06). store.test asserts `error === null`. |
| T-02-03-XSS | Tampering | file-derived strings rendered in the panel | mitigate | React escapes text content by default; only numeric/derived grid fields and fixed labels are rendered — no `dangerouslySetInnerHTML`. |
| T-02-03-SC | Tampering | npm/pip/cargo installs | accept | Plan adds NO packages (02-RESEARCH Package Legitimacy Audit: zero installs). |
</threat_model>

<verification>
- `npx vitest run src/state/store.test.ts` green (imaging + non-imaging paths).
- `npx tsc --noEmit` + `npm run lint` clean.
- `App.tsx` mounts the panel; non-imaging notice + no-error path proven.
</verification>

<success_criteria>
- Loading an imaging file produces a visible, expandable grid diagnostics panel and a non-null `store.grid`; loading a non-imaging file shows the calm muted notice with no error — the complete, demonstrable Phase-2 vertical slice (IMG-01/02/03 wired end-to-end).
</success_criteria>

<output>
Create `.planning/phases/02-imaging-grid-reconstruction-the-gate/02-03-SUMMARY.md` when done.
</output>
