---
phase: 01-reader-foundation-open-and-inspect
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - .planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND1.log
  - .planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND2.log
autonomous: false
requirements: [LOAD-01, LOAD-02, LOAD-03, FMT-01, FMT-02, FMT-03, FMT-04, DATA-01, DATA-02]
must_haves:
  truths:
    - "Codex round1 (plan bundle) and round2 (phase diff) have been run for Phase 1"
    - "Each verdict line (accept / accept-with-revisions / reject) is captured for the phase commit footer"
    - "All Phase-1 success criteria are demonstrably met by the full test suite + build"
  artifacts:
    - path: ".planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND1.log"
      provides: "Adversarial review of the Phase-1 plan bundle"
    - path: ".planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND2.log"
      provides: "Adversarial review of the Phase-1 diff"
  key_links:
    - from: "tools/codex_review.sh"
      to: ".planning/phases/01-reader-foundation-open-and-inspect/"
      via: "round1/round2 write logs here"
      pattern: "01-CODEX-ROUND"
---

<objective>
Final gate for Phase 1 (PROC-01): run the Codex adversarial review harness against this phase's plan bundle (round1) and against the phase diff (round2), capture the verdicts, run the full verification suite, and surface any non-accept verdict to the operator for adjudication.

Purpose: Phase 1 bootstrapped `tools/codex_review.sh` so the phase itself is reviewable. This plan exercises it and gates the phase on a green build + reviewed verdicts. Per CLAUDE.md PROC-01, Claude runs the CLI; the operator decides what to do with non-accept verdicts.
Output: Two Codex log files + verdict lines for the phase commit footer; a confirmed-green Phase-1 deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/01-reader-foundation-open-and-inspect/01-CONTEXT.md

<depends_on_summary>
@.planning/phases/01-reader-foundation-open-and-inspect/01-01-SUMMARY.md
@.planning/phases/01-reader-foundation-open-and-inspect/01-02-SUMMARY.md
@.planning/phases/01-reader-foundation-open-and-inspect/01-03-SUMMARY.md
</depends_on_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase-1 verification sweep</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria #1-#5)
    - .planning/phases/01-reader-foundation-open-and-inspect/01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md (what each plan delivered + verify commands)
  </read_first>
  <action>
    Run the complete Phase-1 verification: `npm ci` (clean install with submodule bootstrap), `npm run lint`, `npx tsc --noEmit`, `npm test` (all Vitest suites incl. reader/arrays/capability/stats/store), `npm run build`, and `npx playwright test e2e/skeleton.spec.ts` against `vite preview`. Confirm against ROADMAP Phase-1 success criteria: (1) local picker+drag-drop+URL with staged progress; (2) manifest + 5 metadata groups; (3) counts + m/z range + MS levels + capabilities incl. imaging-detected; (4) point + chunked/delta single-spectrum reconstruction; (5) unsupported-encoding -> named actionable error, no silent zeros. Record any failure as a blocker (do not paper over) before invoking Codex.
  </action>
  <verify>
    <automated>npm ci && npm run lint && npx tsc --noEmit && npm test && npm run build && npx playwright test e2e/skeleton.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the verify line exits 0.
    - Each of the five ROADMAP Phase-1 success criteria maps to at least one passing test or build artifact (note the mapping in the SUMMARY).
    - `grep -rl "from ['\"]mzpeakts['\"]" src/` returns only files under `src/reader/` (boundary held across all plans).
  </acceptance_criteria>
  <done>Full suite green; all five Phase-1 success criteria demonstrably met.</done>
</task>

<task type="auto">
  <name>Task 2: Run Codex round1 (plan bundle) and round2 (phase diff)</name>
  <read_first>
    - CLAUDE.md (PROC-01: Claude runs tools/codex_review.sh round1/round2; copies verdict into the phase commit footer; only escalate on reject or substantive accept-with-revisions; codex CLI at /opt/homebrew/bin/codex; logs gitignored)
    - tools/codex_review.sh (the harness bootstrapped in 01-01)
  </read_first>
  <action>
    Determine the phase-start SHA (the commit immediately before Phase-1 execution began; record it in the SUMMARY). Run `bash tools/codex_review.sh round1 01` (adversarial read of the Phase-1 plan bundle) and `bash tools/codex_review.sh round2 01 --sha <phase_start_sha>` (adversarial read of the phase diff). Capture the verdict line from each (`accept` / `accept-with-revisions` / `reject`) and record both in the SUMMARY for the phase commit footer. Logs land at `.planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND{1,2}.log` (gitignored).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND1.log && test -f .planning/phases/01-reader-foundation-open-and-inspect/01-CODEX-ROUND2.log</automated>
  </verify>
  <acceptance_criteria>
    - Both round1 and round2 logs exist and contain a recognizable verdict line.
    - Both verdict lines are recorded in 01-04-SUMMARY.md for the commit footer.
  </acceptance_criteria>
  <done>Both Codex rounds run; verdicts captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 1 complete: scaffold + vendored reader + open-and-inspect (local/URL + staged progress), full metadata/manifest/stats/capabilities, point + chunked/delta single-spectrum view, fail-loud capability detection, and Codex review verdicts.</what-built>
  <how-to-verify>
    1. Review the two Codex verdict lines reported in 01-04-SUMMARY.md (round1 plan, round2 diff).
    2. If both are `accept`, approve to close the phase.
    3. If either is `reject` or a substantive `accept-with-revisions`, decide whether to address now (loop back via gap closure) or accept the noted risk.
    4. Optionally run `npm run preview` and open a demo `.mzpeak` (URL or local) to eyeball the metadata + spectrum view yourself.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 1, or describe required revisions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex CLI review of plan/diff | Advisory adversarial review; operator adjudicates verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-REPUD | Repudiation | unrecorded review verdict | mitigate | Both verdict lines captured in the SUMMARY for the phase commit footer (PROC-01). |
| T-01-04-SC | Tampering | no new package installs | accept | Review/gate plan; adds no dependencies. |
</threat_model>

<verification>
- Full suite green (Task 1).
- Both Codex logs present with verdict lines (Task 2).
- Operator approval recorded (Task 3 checkpoint).
</verification>

<success_criteria>
- Phase 1 is reviewed (PROC-01 round1 + round2), green across lint/types/tests/build/e2e, and approved by the operator.
</success_criteria>

<output>
Create `.planning/phases/01-reader-foundation-open-and-inspect/01-04-SUMMARY.md` when done, including both Codex verdict lines and the phase-start SHA.
</output>
