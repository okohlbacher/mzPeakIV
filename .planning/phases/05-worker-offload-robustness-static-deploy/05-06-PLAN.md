---
phase: 05-worker-offload-robustness-static-deploy
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/mzPeakWorker.ts
  - src/worker/protocol.ts
autonomous: true
gap_closure: true
requirements:
  - UX-01

must_haves:
  truths:
    - "After runLoadInWorker completes, activeGrid.presenceMask remains a valid, non-detached Uint8Array"
    - "computeIonImageStats(ionImage, activeGrid) reads correct presence values for all subsequent renderIonImage calls"
    - "The stale comment 'tic and presenceMask are now detached' is gone — replaced with accurate 'tic.buffer is now detached'"
  artifacts:
    - path: "src/worker/mzPeakWorker.ts"
      provides: "presenceMask NOT in transfer list; updated post-transfer comment"
      contains: "tic.buffer is now detached"
    - path: "src/worker/protocol.ts"
      provides: "LoadResult JSDoc without presenceMask transfer note"
  key_links:
    - from: "src/worker/mzPeakWorker.ts renderIonImage handler"
      to: "src/compute/ionImage.ts computeIonImageStats"
      via: "activeGrid.presenceMask (must be valid Uint8Array, byteLength > 0)"
      pattern: "computeIonImageStats\\(ionImage, activeGrid\\)"
---

<objective>
Fix CR-01: remove presenceMask.buffer from the Worker's transfer list so activeGrid retains a valid, non-detached Uint8Array for all subsequent renderIonImage calls.

Purpose: The presenceMask.buffer transfer silently detaches activeGrid.presenceMask after the first file load. Every subsequent computeIonImageStats call reads undefined from the detached buffer, producing wrong nonzeroCount/min/max values and corrupting spatial layout rendering.

Output: src/worker/mzPeakWorker.ts with the transfer list fix and accurate comments; src/worker/protocol.ts with updated LoadResult JSDoc.
</objective>

<execution_context>
@/Users/kohlbach/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohlbach/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove presenceMask.buffer from transfer list and fix comments</name>

  <read_first>
    src/worker/mzPeakWorker.ts lines 212-245 (module-scope assignment + transfer list + post-transfer comment)
    src/worker/protocol.ts lines 76-98 (LoadResult type with presenceMask transfer note in JSDoc)
  </read_first>

  <files>src/worker/mzPeakWorker.ts, src/worker/protocol.ts</files>

  <action>
    In src/worker/mzPeakWorker.ts, make these changes:

    1. Remove the presenceMask transfer from the transfer list block (lines 218-223). Replace the three-line comment + presenceMask push with:

       const transferList: Transferable[] = [];
       if (tic) transferList.push(tic.buffer);
       // NOTE: presenceMask.buffer is NOT transferred — activeGrid retains a valid
       // buffer for subsequent renderIonImage calls. The Uint8Array is small enough
       // (~35 KB for PXD001283 260x134) that structured-clone copy cost is negligible.

       The removed lines are:
         - "// Build the transfer list: transfer tic.buffer zero-copy (Pattern 3 / Pitfall 2)."
         - "// CRITICAL: always [float32Array.buffer] not [float32Array]."
         - "// Also transfer presenceMask.buffer from grid (ImagingGrid note in protocol.ts)."
         - "if (grid!.presenceMask) transferList.push(grid!.presenceMask.buffer);"

    2. Fix the post-transfer warning comment at line 240. Replace:
         // WARNING: tic and presenceMask are now detached — do not use after postMessage.
       with:
         // WARNING: tic.buffer is now detached — do not use tic after postMessage.

    In src/worker/protocol.ts, update the LoadResult JSDoc block (lines 76-88). The comment currently says:
       "Both survive structured clone (Maps are cloneable; TypedArrays are
       transferable). The Map is deep-cloned (not zero-copy); Uint8Array.buffer
       can be transferred. Plan 02 (Worker implementation) should add
       presenceMask.buffer to the transfer list to avoid a copy of that array."

    Replace the Uint8Array transfer note with:
       "Both survive structured clone (Maps are cloneable; TypedArrays are
       cloneable). The Map is deep-cloned (not zero-copy); the presenceMask
       Uint8Array is structured-cloned (small, ~35 KB). presenceMask.buffer is
       intentionally NOT transferred — the Worker retains a valid presenceMask
       for subsequent renderIonImage calls."
  </action>

  <verify>
    <automated>grep "presenceMask.buffer" /Users/kohlbach/Claude/mzPeakIV/src/worker/mzPeakWorker.ts | wc -l | xargs -I{} test {} -eq 0 && echo "PASS: presenceMask.buffer not in transfer list" || echo "FAIL: presenceMask.buffer still present"</automated>
    <automated>grep "tic.buffer is now detached" /Users/kohlbach/Claude/mzPeakIV/src/worker/mzPeakWorker.ts && echo "PASS: comment updated" || echo "FAIL: stale comment still present"</automated>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx tsc -b 2>&1 | tail -5; echo "TSC EXIT: $?"</automated>
  </verify>

  <done>
    grep "presenceMask.buffer" src/worker/mzPeakWorker.ts returns no output.
    The post-transfer comment reads "tic.buffer is now detached".
    protocol.ts JSDoc no longer references adding presenceMask.buffer to the transfer list.
    npx tsc -b exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → main thread | Structured-clone copy of presenceMask (was zero-copy transfer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | presenceMask Uint8Array | accept | Structured clone produces an independent copy; neither thread can corrupt the other's copy post-message. Lower risk than the transfer path which detached the Worker's copy. |
</threat_model>

<verification>
npm run build must exit 0.
grep "presenceMask.buffer" src/worker/mzPeakWorker.ts must produce no output.
npx tsc -b must exit 0.
</verification>

<success_criteria>
CR-01 closed: activeGrid.presenceMask is a valid Uint8Array (byteLength > 0) after runLoadInWorker completes. computeIonImageStats receives a correct mask for all subsequent renderIonImage calls.
</success_criteria>

<output>
Create `.planning/phases/05-worker-offload-robustness-static-deploy/05-06-SUMMARY.md` when done.
</output>
