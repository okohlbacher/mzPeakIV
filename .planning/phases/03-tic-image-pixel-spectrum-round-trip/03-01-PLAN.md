---
phase: 03-tic-image-pixel-spectrum-round-trip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reader/types.ts
  - src/compute/tic.ts
  - src/compute/tic.test.ts
  - src/ui/rasterize.ts
  - src/ui/rasterize.test.ts
autonomous: true
requirements: [IMAGE-01, IMAGE-04]
must_haves:
  truths:
    - "buildTic sums each spectrum's full intensity array onto the correct grid cell (no flip/transpose)"
    - "Absent pixels (presenceMask===0) render to a distinct sentinel color, never colormap-bottom"
    - "Zero-intensity present pixels render to the colormap bottom, distinct from the sentinel"
    - "Rasterize output ignores absent cells when computing the 99th-percentile clip ceiling"
  artifacts:
    - path: "src/compute/tic.ts"
      provides: "buildTic(xic, grid) -> Float32Array pure aggregation"
      exports: ["buildTic"]
      contains: "intensity array"
    - path: "src/compute/tic.test.ts"
      provides: "synthetic-fixture unit tests for buildTic"
    - path: "src/ui/rasterize.ts"
      provides: "rasterizeTic(tic, grid) -> Uint8ClampedArray (colormap + clip + sparse sentinel)"
      exports: ["rasterizeTic", "viridis"]
    - path: "src/ui/rasterize.test.ts"
      provides: "unit tests for colormap, percentile clip, sparse sentinel"
    - path: "src/reader/types.ts"
      provides: "LoadStage union with 'tic' member"
      contains: "\"tic\""
  key_links:
    - from: "src/compute/tic.ts"
      to: "src/imaging/types.ts ImagingGrid.coordToSpectrumIndex"
      via: "reverse map spectrumIndex->key (key = y0*width+x0)"
      pattern: "coordToSpectrumIndex"
    - from: "src/ui/rasterize.ts"
      to: "src/imaging/types.ts ImagingGrid.presenceMask"
      via: "sparse sentinel branch"
      pattern: "presenceMask"
---

<objective>
Build the two pure, DOM-free foundations of the TIC slice: the compute aggregation `buildTic` (intensity-sum → `Float32Array` raster) and the render transform `rasterizeTic` (colormap + 99th-percentile clip + sparse sentinel → `Uint8ClampedArray`). Add the `'tic'` member to the `LoadStage` union so the store stage (plan 03-03) and `App.tsx` can reference it.

Purpose: These are the testable correctness surfaces of IMAGE-01 (TIC sum) and IMAGE-04 (fixed orientation + sparse-vs-zero distinction). Extracting them out of any React component (RESEARCH Pitfall 4) lets Vitest prove orientation, the bigint boundary, the reverse-map scatter, the percentile clip, and the absent≠zero rule without a canvas mock.
Output: `src/compute/tic.ts` + test, `src/ui/rasterize.ts` + test, and a one-line `LoadStage` extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CONTEXT.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-RESEARCH.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-PATTERNS.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-UI-SPEC.md
@.planning/research/IMAGING-SPEC-ALIGNMENT.md

<interfaces>
<!-- The exact contracts the executor builds against. Do NOT explore the codebase to rediscover these. -->

From vendor/mzpeakts/lib/src/reader.ts (the XIC shape returned by extractXIC):
```
export interface XICPoint { index: bigint; time: number | null; dataArrays: DataArrays; }
export interface XIC { points: XICPoint[]; target: { timeRange; mzRange }; }
```
`DataArrays` is a `Record<string, ...>` keyed by human-readable CV name. The intensity
column key is the string "intensity array" (same constant src/reader/arrays.ts uses).
`XICPoint.index` is a `bigint` — convert with `Number()` at the FIRST op (Pitfall 1).

From src/imaging/types.ts (already shipped by Phase 2 — consume, do not modify):
```
export interface ImagingGrid {
  width: number; height: number; coordinateBase: number;
  pixelSizeUm: { x: number; y: number } | null;
  coordToSpectrumIndex: Map<number, number>;  // key = y0*width + x0 (row-major, 0-based)
  presenceMask: Uint8Array;                    // length width*height; 1 = filled, 0 = absent
  filledCount: number; totalCells: number;
  coordSourceStrategy; diagnostics;
}
```

From src/reader/types.ts (the LoadStage union to extend):
```
export type LoadStage = "idle" | "zip-index" | "manifest" | "metadata" | "grid" | "ready" | "error";
```

From src/reader/arrays.ts (the intensity key constant to reuse verbatim):
```
const INTENSITY_KEY = "intensity array";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 'tic' LoadStage + buildTic compute module with synthetic-fixture tests</name>
  <files>src/reader/types.ts, src/compute/tic.ts, src/compute/tic.test.ts</files>
  <read_first>
    - src/reader/types.ts (the LoadStage union being extended — read current members)
    - src/imaging/types.ts (ImagingGrid shape — buildTic consumes coordToSpectrumIndex, presenceMask, width, height)
    - src/imaging/grid.ts (the authoritative key formula `key = y0*width + x0` at line ~116 — buildTic must REUSE by inverting the map, never re-derive; copy its module-header boundary-rule comment style)
    - src/imaging/grid.test.ts (the synthetic-fixture test style: denseCoords helper, describe/it naming, NO reader mock, NO binary fixture)
    - src/reader/arrays.ts (the INTENSITY_KEY = "intensity array" constant to reuse)
    - vendor/mzpeakts/lib/src/reader.ts lines 20-32 (XICPoint/XIC types — bigint index, dataArrays)
  </read_first>
  <behavior>
    - Test 1: buildTic on a dense 3x2 grid + matching XIC sums each point's "intensity array" onto cell key = y0*width+x0; assert tic[key] equals the known per-spectrum sum.
    - Test 2: orientation — a spectrum at grid coord (x=2,y=0 zero-based) lands at index 2 (NOT at the transposed index 0*2+0 of a flipped grid); assert no transpose/flip.
    - Test 3: bigint boundary — XICPoint.index given as a bigint scatters onto the same cell as Number(index); a Map.get with a bigint key would miss, so the conversion must happen.
    - Test 4: sparse skip — a grid with one absent pixel (presenceMask[k]===0, not in coordToSpectrumIndex) leaves tic[k]===0; an XIC point whose spectrum index is not on the grid is skipped (no throw).
    - Test 5: missing/empty intensity array — a point with no "intensity array" key contributes nothing (no NaN, no throw); a non-finite element is treated as 0 (Number.isFinite guard).
  </behavior>
  <action>
    First extend `src/reader/types.ts`: insert the string literal `"tic"` into the `LoadStage` union between `"grid"` and `"ready"`, with an inline comment "Phase 3 TIC compute stage (D-02)".

    Then create `src/compute/tic.ts` as a new sibling layer above `src/reader/` and `src/imaging/`. Copy the module-header boundary-rule comment convention from `src/imaging/grid.ts` (state: this is a pure transform, cites D-03, NEVER touches Arrow/bigint/mzpeakts beyond the documented `Number()` conversion at the boundary). Export `function buildTic(xic: XIC, grid: ImagingGrid): Float32Array`. Import the `XIC`/`XICPoint` types from the vendored reader for the parameter type only (type-only import is acceptable here as the documented single boundary conversion point; if a type-only import from vendor is disallowed by lint, define a local structural type `{ points: { index: bigint | number; dataArrays: Record<string, ArrayLike<number> | undefined> }[] }` and accept that — prefer the local structural type to keep `src/compute/` free of vendor imports). Implementation: allocate `const tic = new Float32Array(grid.width * grid.height)`; build the reverse map `idxToKey: Map<number, number>` by iterating `grid.coordToSpectrumIndex` (`for (const [key, sIdx] of grid.coordToSpectrumIndex) idxToKey.set(sIdx, key)`); reuse the constant `const INTENSITY_KEY = "intensity array"`; for each point compute `const sIdx = Number(point.index)` as the FIRST op, look up `const key = idxToKey.get(sIdx)`, `continue` when undefined, read `const arr = point.dataArrays[INTENSITY_KEY]`, `continue` when falsy, sum its elements with a `Number.isFinite(arr[i]) ? arr[i] : 0` guard, and assign `tic[key] = sum`. Do NOT introduce any transpose/flip — the key formula is owned by `grid.ts` (C2 MANDATORY, IMAGE-04).

    Then create `src/compute/tic.test.ts` mirroring `src/imaging/grid.test.ts`: a `makeGrid(width, height, absent?: number[])` helper that builds a minimal `ImagingGrid` (only the fields buildTic reads: width, height, coordToSpectrumIndex, presenceMask, plus stub values for the rest) and a `makeXic(perSpectrumIntensity: number[][])` helper producing `{ points: [...] }` with `bigint` indices. Implement Tests 1-5 from the behavior block. Use the `(Test N)` suffix in `it(...)` names and the plan-binding comment header from grid.test.ts.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx vitest run src/compute/tic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/reader/types.ts` LoadStage union contains the member `"tic"` (grep `"tic"` in the union)
    - `src/compute/tic.ts` exports `buildTic` and contains the literal `"intensity array"`
    - `src/compute/tic.ts` contains `Number(point.index)` (bigint boundary conversion) and inverts `coordToSpectrumIndex` (grep `coordToSpectrumIndex`)
    - `src/compute/tic.ts` contains NO `import` from `vendor/` or `apache-arrow` or `mzpeakts` runtime (type-only OR local structural type only) — grep for `from "apache-arrow"` and `from "mzpeakts"` returns nothing
    - `npx vitest run src/compute/tic.test.ts` exits 0 with all 5 tests passing
  </acceptance_criteria>
  <done>buildTic sums correctly, scatters via the inverted grid map with no flip, converts the bigint index, skips off-grid/absent/empty points, and the LoadStage union carries "tic". All compute tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rasterizeTic pure render helper (viridis colormap + 99th-pct clip + sparse sentinel) with tests</name>
  <files>src/ui/rasterize.ts, src/ui/rasterize.test.ts</files>
  <read_first>
    - src/compute/tic.ts (the Float32Array TIC raster this consumes — from Task 1)
    - src/imaging/types.ts (ImagingGrid.presenceMask, width, height)
    - src/imaging/grid.ts (the Number.isFinite guard discipline to carry over for non-finite sums)
    - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-UI-SPEC.md (Canvas data colors table: viridis-like LUT default; sentinel #1a1a1a RGBA 26,26,26,255; sentinel must be visually distinct from every colormap value)
    - .planning/research/IMAGING-SPEC-ALIGNMENT.md (C8 sparse presence mask; D-09 absent != zero)
  </read_first>
  <behavior>
    - Test 1: rasterizeTic returns a Uint8ClampedArray of length width*height*4 (RGBA per cell).
    - Test 2: an absent cell (presenceMask[k]===0) maps to the sentinel RGBA (26,26,26,255), NOT to colormap(0); assert it differs from the value a zero-intensity present cell produces.
    - Test 3: a present zero-intensity cell (presenceMask[k]===1, tic[k]===0) maps to colormap(0), distinct from the sentinel.
    - Test 4: percentile clip ignores absent cells — given values where the only large value sits in an absent cell, the clip ceiling derives only from present cells; the brightest present cell normalizes to 1.0 (top of colormap).
    - Test 5: non-finite or negative tic value on a present cell clamps to normalized [0,1] (no out-of-range RGBA, no NaN in output).
    - Test 6 (orientation passthrough): rasterizeTic does NOT reorder cells — output cell k corresponds to input tic[k] (RGBA at offset k*4 derives from tic[k]); no transpose.
  </behavior>
  <action>
    Create `src/ui/rasterize.ts` as a pure DOM-free helper (RESEARCH Pitfall 4; UI-SPEC permits the pure split). Copy the `src/imaging/grid.ts` finite-guard discipline. Export `function viridis(norm: number): [number, number, number]` — a single fixed perceptually-uniform LUT approximation (implement as a small fixed array of ~8-16 RGB anchor stops with linear interpolation between them, OR a closed-form viridis approximation; either is acceptable — keep it ONE swappable pure function so Phase 4 can add a selector without refactor, per UI-SPEC and D-discretion). Export `function rasterizeTic(tic: Float32Array, grid: ImagingGrid): Uint8ClampedArray`. Implementation: allocate `const out = new Uint8ClampedArray(grid.width * grid.height * 4)`; compute `const clipMax = percentile99(tic, grid.presenceMask)` where `percentile99` considers ONLY cells with `presenceMask[k] !== 0` (sort the present-cell values, take the value at the 0.99 index; guard empty → 0); define the sentinel constant `const SENTINEL: [number,number,number] = [0x1a, 0x1a, 0x1a]`; loop `k` over `width*height`: when `presenceMask[k] === 0` write the sentinel RGBA (alpha 255) and `continue`; otherwise compute `const raw = tic[k]; const norm = clipMax > 0 && Number.isFinite(raw) ? Math.min(Math.max(raw / clipMax, 0), 1) : 0`, call `viridis(norm)`, write RGBA (alpha 255). Do NOT reorder cells — output offset `k*4` corresponds to input `tic[k]` (orientation is already correct upstream).

    Create `src/ui/rasterize.test.ts`: a `makeGrid` helper (reuse the shape from tic.test.ts — width, height, presenceMask) and Tests 1-6 from the behavior block. Assert exact sentinel RGBA bytes, assert the brightest present cell hits the top of the LUT, assert clamp on non-finite/negative inputs.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx vitest run src/ui/rasterize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/rasterize.ts` exports `rasterizeTic` and `viridis`
    - `src/ui/rasterize.ts` contains the sentinel bytes `0x1a` (or decimal 26) and writes them only on the `presenceMask[k] === 0` branch (grep `presenceMask`)
    - `src/ui/rasterize.ts` percentile computation filters by presence mask (grep shows the percentile function reads `presenceMask`)
    - `src/ui/rasterize.ts` has NO `import` from `react`, `uplot`, or any DOM/canvas API (pure function) — grep for `from "react"` returns nothing
    - `npx vitest run src/ui/rasterize.test.ts` exits 0 with all 6 tests passing
  </acceptance_criteria>
  <done>rasterizeTic produces correct RGBA with the sparse sentinel distinct from colormap-zero, a presence-mask-filtered 99th-percentile clip, clamped normalization, and no cell reordering. All rasterize tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mzpeak` file → reader → XIC arrays | Untrusted file content reaches `buildTic` as numeric arrays via the reader boundary. |
| TIC `Float32Array` → rasterizeTic | Computed numeric values become RGBA bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | buildTic intensity sum | mitigate | `Number.isFinite` guard per element treats NaN/Infinity as 0 (V5); no OOB write — `tic` is sized to the already-`MAX_CELLS`-capped grid. |
| T-03-02 | Denial of Service | buildTic allocation | accept | `tic` is `width*height` of an already-capped `ImagingGrid` (grid.ts enforces `MAX_CELLS`); no new unbounded allocation introduced. |
| T-03-03 | Tampering | rasterizeTic normalization | mitigate | `Math.min(Math.max(raw/clipMax,0),1)` clamps non-finite/negative to a valid `[0,1]`; `Uint8ClampedArray` clamps byte writes — no broken colormap, no OOB RGBA. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH: zero dependencies). slopcheck N/A. |
</threat_model>

<verification>
- `npx vitest run src/compute/tic.test.ts src/ui/rasterize.test.ts` — both suites pass.
- `npx tsc --noEmit` — no type errors introduced by the new modules or the LoadStage extension.
- grep confirms no Arrow/mzpeakts runtime imports in `src/compute/tic.ts` and no React/DOM imports in `src/ui/rasterize.ts`.
</verification>

<success_criteria>
- `buildTic` sums per-spectrum intensity onto the correct (non-flipped) grid cell, converts the bigint index, and skips off-grid/absent points.
- `rasterizeTic` renders absent pixels distinct from zero-intensity pixels and clips to the 99th percentile of present cells.
- `LoadStage` carries `"tic"`.
- All unit tests green; no boundary violations.
</success_criteria>

<output>
Create `.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-01-SUMMARY.md` when done.
</output>
