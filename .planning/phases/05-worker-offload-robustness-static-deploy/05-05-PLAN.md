---
phase: 05-worker-offload-robustness-static-deploy
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified: []
autonomous: false
requirements:
  - UX-01

must_haves:
  truths:
    - "Codex round1 adversarial review of the Phase 5 plan bundle is complete"
    - "Codex round2 adversarial review of the Phase 5 diff is complete"
    - "Both verdict lines (accept / accept-with-revisions / reject) are recorded"
    - "Non-accept verdicts are escalated to the operator for adjudication"
  artifacts:
    - path: ".planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND1.log"
      provides: "Round1 adversarial review output"
    - path: ".planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND2.log"
      provides: "Round2 adversarial review output"
  key_links:
    - from: "tools/codex_review.sh"
      to: ".planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND1.log"
      via: "round1 invocation"
      pattern: "CODEX-ROUND1"
---

<objective>
Execute the PROC-01 Codex adversarial review for Phase 5 per the project mandate. Run round1 (adversarial read of the phase PLAN bundle before all plans are executed, or after if round1 was not done pre-execution) and round2 (adversarial read of the phase DIFF since the phase-start SHA). Record both verdict lines in the phase commit footer.

Purpose: PROC-01 is a project-level mandate (CLAUDE.md §PROC-01). Every phase must be bracketed by an adversarial review. Phase 5 also finalizes the review harness itself (codex_review.sh hardened in Plan 04).

Output: Two log files, two verdict lines, operator adjudication of any non-accept verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-worker-offload-robustness-static-deploy/05-CONTEXT.md
@.planning/phases/05-worker-offload-robustness-static-deploy/05-04-SUMMARY.md

<interfaces>
<!-- tools/codex_review.sh usage: -->
<!--   round1: bash tools/codex_review.sh round1 05                     -->
<!--   round2: bash tools/codex_review.sh round2 05 --sha <start_sha>   -->
<!--   dry-run: add --dry-run flag to test without live codex call       -->
<!-- Log files land at .planning/phases/05-*/05-CODEX-ROUND{1,2}.log    -->
<!-- (gitignored — operator reads but does not commit them)              -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Codex round1 and round2 reviews</name>
  <files></files>
  <read_first>
    - tools/codex_review.sh — confirm the round1 and round2 syntax before invoking
    - CLAUDE.md §PROC-01 — review convention and commit footer format
  </read_first>
  <action>
    Determine the phase-start SHA: the SHA of the last commit BEFORE Phase 5 work began (the commit just before any 05-* plan or implementation was committed). Use `git log --oneline` to identify this SHA.

    Run round1 review:
      bash tools/codex_review.sh round1 05
    Capture the verdict line from stdout.

    Run round2 review with the phase-start SHA:
      bash tools/codex_review.sh round2 05 --sha <phase_start_sha>
    Capture the verdict line from stdout.

    If the codex binary is not available at /opt/homebrew/bin/codex, use --dry-run to verify the harness works and note in the summary that a live review could not be performed.

    Record both verdict lines in the plan output below.
  </action>
  <verify>
    <automated>ls /Users/kohlbach/Claude/mzPeakIV/.planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND1.log 2>/dev/null && echo "round1 log exists" || echo "round1 not yet run"</automated>
  </verify>
  <acceptance_criteria>
    - tools/codex_review.sh round1 05 completes (live or dry-run)
    - tools/codex_review.sh round2 05 --sha <sha> completes (live or dry-run)
    - Both verdict lines are captured and recorded in the summary
    - Log files exist at the expected paths (or dry-run confirmation recorded)
  </acceptance_criteria>
  <done>Both Codex reviews complete; verdict lines recorded for the phase commit footer</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Codex round1 and round2 adversarial reviews have been run per PROC-01. Both verdict lines are recorded in the summary and log files.</what-built>
  <how-to-verify>
    1. Review the verdict lines from the task above (accept / accept-with-revisions / reject).
    2. If BOTH verdicts are "accept": no action needed — proceed to commit.
    3. If either verdict is "accept-with-revisions": review the specific revisions listed in the log file. Decide whether to address them now or note them as deferred with rationale.
    4. If either verdict is "reject": escalate — work cannot be considered complete until the rejection is addressed.
    5. Copy both verdict lines into the next git commit's footer:
         codex-round1: accept
         codex-round2: accept-with-revisions
       (or whatever the actual verdicts are)
  </how-to-verify>
  <resume-signal>Type "approved" if both verdicts are accept, or describe the verdict and whether revisions will be addressed or deferred.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codex CLI → phase logs | Review output written to gitignored log files; verdict line printed to stdout only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | codex_review.sh invocation with wrong SHA | mitigate | SHA confirmed from git log before round2 invocation; hardened empty-diff detection (Plan 04) will error if wrong |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No installs; codex binary at fixed path |
</threat_model>

<verification>
After both tasks complete:
1. .planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND1.log exists (or dry-run confirmed)
2. .planning/phases/05-worker-offload-robustness-static-deploy/05-CODEX-ROUND2.log exists (or dry-run confirmed)
3. Both verdict lines recorded in 05-05-SUMMARY.md
4. Operator has confirmed the verdicts and any required adjudication
</verification>

<success_criteria>
- Both PROC-01 review rounds executed
- Verdict lines captured and ready for the phase commit footer
- Non-accept verdicts adjudicated or formally deferred with rationale
- Phase 5 complete when this checkpoint is approved
</success_criteria>

<output>
Create `.planning/phases/05-worker-offload-robustness-static-deploy/05-05-SUMMARY.md` when done.
Record both verdict lines in the summary for inclusion in the phase commit footer.
</output>
