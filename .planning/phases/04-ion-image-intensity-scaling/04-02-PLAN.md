---
phase: "04-ion-image-intensity-scaling"
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/rasterize.ts
  - src/ui/rasterize.test.ts
autonomous: true
requirements:
  - IMAGE-03
must_haves:
  truths:
    - "rasterizeImage generalizes rasterizeTic with colormap, percentile, and logScale parameters"
    - "rasterizeTic remains a thin wrapper — all existing Phase 3 callers and tests continue passing"
    - "Inferno and Grayscale LUTs are added alongside viridis"
    - "Log scaling uses Math.log1p so raw=0 → norm 0 exactly (never NaN or negative)"
    - "percentileClip accepts a parameter p instead of hardcoding 0.99"
    - "Absent pixels (presenceMask===0) render as SENTINEL regardless of colormap"
  artifacts:
    - path: src/ui/rasterize.ts
      provides: "rasterizeImage, Colormap type, RasterizeOpts interface, inferno LUT; rasterizeTic thin wrapper"
      exports:
        - Colormap
        - RasterizeOpts
        - rasterizeImage
        - rasterizeTic
        - viridis
        - inferno
    - path: src/ui/rasterize.test.ts
      provides: "extended tests for log scaling, percentile param, inferno, gray, sentinel invariance"
  key_links:
    - from: src/ui/rasterize.ts
      to: src/imaging/types.ts
      via: "import type { ImagingGrid }"
      pattern: "import type.*ImagingGrid.*from.*imaging/types"
    - from: src/ui/ImagingPanel.tsx
      to: src/ui/rasterize.ts
      via: "rasterizeTic — must remain callable with same signature"
      pattern: "rasterizeTic.*tic.*grid"
---

<objective>
Generalize src/ui/rasterize.ts from a single-purpose TIC rasterizer into a
parameterized rasterizer supporting colormaps (Viridis/Inferno/Grayscale), percentile
clip (90/95/99/99.9th), and linear/log intensity scaling — while keeping rasterizeTic
as a thin default-args wrapper so Phase 3 callers and tests don't break.

Purpose: IMAGE-03 (colormap + scaling). This pure-function layer must be finished
before the store slice (Plan 03) and UI components (Plans 04–05) can wire the controls.

Output: Extended src/ui/rasterize.ts with new exports; extended src/ui/rasterize.test.ts.
</objective>

<execution_context>
@/Users/kohlbach/Claude/get-shit-done/workflows/execute-plan.md
@/Users/kohlbach/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ion-image-intensity-scaling/04-CONTEXT.md
@.planning/phases/04-ion-image-intensity-scaling/04-RESEARCH.md
@.planning/phases/04-ion-image-intensity-scaling/04-PATTERNS.md
@.planning/phases/04-ion-image-intensity-scaling/04-UI-SPEC.md

<interfaces>
<!-- Key interfaces extracted from src/ui/rasterize.ts (current file to be extended). -->

Current exports (must remain intact):
```typescript
// src/ui/rasterize.ts — existing exports
export function viridis(norm: number): [number, number, number]
export function rasterizeTic(tic: Float32Array, grid: ImagingGrid): Uint8ClampedArray

// Internal (not exported, to be renamed/parameterized):
function percentile99(tic: Float32Array, presenceMask: Uint8Array): number
// → rename to: function percentileClip(values: Float32Array, presenceMask: Uint8Array, p: number): number

// Existing constant (must be reused in rasterizeImage):
const SENTINEL: readonly [number, number, number] = [0x1a, 0x1a, 0x1a];

// Existing stop structure (template for INFERNO_STOPS):
const VIRIDIS_STOPS: ReadonlyArray<readonly [number, number, number]> = [
  [68, 1, 84],    // 0.000 stop
  // ... 9 stops total, 8 segments
  [253, 231, 37], // 1.000 stop
];
```

New exports to add:
```typescript
export type Colormap = "viridis" | "inferno" | "gray";
export interface RasterizeOpts { colormap: Colormap; percentile: number; logScale: boolean; }
export function rasterizeImage(values: Float32Array, grid: ImagingGrid, opts: RasterizeOpts): Uint8ClampedArray
export function inferno(norm: number): [number, number, number]
// rasterizeTic becomes: rasterizeImage(tic, grid, { colormap: "viridis", percentile: 0.99, logScale: false })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend src/ui/rasterize.ts — add Inferno/Gray LUTs, rasterizeImage, parameterize percentileClip</name>
  <files>src/ui/rasterize.ts</files>
  <read_first>
    - src/ui/rasterize.ts (read the FULL file — know the exact line numbers of percentile99, rasterizeTic, SENTINEL, VIRIDIS_STOPS, viridis before touching anything)
    - src/ui/ImagingPanel.tsx (verify it calls rasterizeTic so you preserve the wrapper)
    - .planning/phases/04-ion-image-intensity-scaling/04-PATTERNS.md lines 154–224 (exact LUT structure and rasterizeImage body)
    - .planning/phases/04-ion-image-intensity-scaling/04-RESEARCH.md Pattern 4 (rasterizeImage full pseudocode + log formula)
  </read_first>
  <behavior>
    - rasterizeImage with colormap "viridis", percentile 0.99, logScale false → same pixel output as the old rasterizeTic (regression test)
    - rasterizeImage with colormap "gray" → R == G == B for every present pixel
    - rasterizeImage with logScale true, raw=0 → norm=0 (no NaN, no negative)
    - rasterizeImage with logScale true, raw=clipMax → norm=1.0 (within floating-point tolerance)
    - rasterizeImage with colormap "inferno" → monotonic luminance (brighter pixels have higher t)
    - rasterizeImage with any colormap → absent cells (presenceMask===0) render as [0x1a, 0x1a, 0x1a, 255]
    - rasterizeTic(tic, grid) still callable with same two-arg signature, output unchanged
    - percentileClip with p=0.90 produces a lower clip ceiling than p=0.99 for the same values
  </behavior>
  <action>
    Edit src/ui/rasterize.ts in place. Do NOT rewrite the file from scratch — read it first and make targeted additions/edits:

    STEP 1 — Add INFERNO_STOPS constant after VIRIDIS_STOPS. Use the standard matplotlib inferno 9 evenly-spaced anchors (same ReadonlyArray structure as VIRIDIS_STOPS). Stops from dark-to-bright: [0,0,4], [40,11,84], [101,21,110], [159,42,99], [212,72,66], [245,125,21], [250,193,39], [252,255,164]. Pad or adjust to exactly 9 entries matching the VIRIDIS_STOPS pattern (8 interpolation segments). These are cosmetic [ASSUMED] values — mark with a comment.

    STEP 2 — Add `export function inferno(norm: number): [number, number, number]` after viridis(), using the same clamped/interpolated LUT pattern — identical code shape, different stops constant.

    STEP 3 — Add `export type Colormap = "viridis" | "inferno" | "gray"` and `export interface RasterizeOpts { colormap: Colormap; percentile: number; logScale: boolean; }`.

    STEP 4 — Rename `percentile99` to `percentileClip` (not exported) and change the hardcoded `0.99` to the parameter `p`. The rest of the function body is unchanged. Update the single call site inside rasterizeTic to `percentileClip(tic, presenceMask, 0.99)` — no behavioral change.

    STEP 5 — Add `export function rasterizeImage(values: Float32Array, grid: ImagingGrid, opts: RasterizeOpts): Uint8ClampedArray`. Body: compute `clipMax = percentileClip(values, presenceMask, opts.percentile)`. For log scale, `denom = Math.log1p(clipMax)`; for linear, `denom = clipMax`. In the pixel loop: if `presenceMask[k] === 0`, emit SENTINEL + alpha 255 and continue. Otherwise: `raw = values[k]`; if `opts.logScale`, `v = Math.log1p(raw)` and `norm = (denom > 0 && raw > 0) ? Math.min(v / denom, 1) : 0`; if linear, `norm = (denom > 0 && Number.isFinite(raw)) ? Math.min(Math.max(raw / denom, 0), 1) : 0`. Select colormap via `switch(opts.colormap)`: "inferno" → inferno(norm), "gray" → `const g = Math.round(Math.min(Math.max(norm, 0), 1) * 255); [g, g, g]`, default → viridis(norm). Write [r, g, b, 255] to output.

    STEP 6 — Replace the body of `rasterizeTic` with `return rasterizeImage(tic, grid, { colormap: "viridis", percentile: 0.99, logScale: false })`. Keep the same exported signature `(tic: Float32Array, grid: ImagingGrid): Uint8ClampedArray`.

    C2 MANDATORY: rasterizeImage uses the same linear `k` index over `grid.width * grid.height` — no flip, no transpose, exactly as the existing rasterizeTic pixel loop does.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npm run build 2>&1 | grep -v "^>" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/rasterize.ts exports: Colormap (type), RasterizeOpts (interface), rasterizeImage (function), inferno (function) — in addition to existing viridis and rasterizeTic
    - File contains `export type Colormap = "viridis" | "inferno" | "gray"`
    - File contains `export interface RasterizeOpts`
    - rasterizeTic body is a single `return rasterizeImage(...)` call (no duplicated pixel loop)
    - File contains `Math.log1p` for the log-scale branch
    - File contains `percentileClip` (renamed from percentile99); the old name percentile99 does NOT appear
    - npm run build exits 0 (no type errors in rasterize.ts or its callers)
    - ImagingPanel.tsx import of rasterizeTic still resolves (name unchanged)
  </acceptance_criteria>
  <done>rasterize.ts compiles cleanly; exports Colormap, RasterizeOpts, rasterizeImage, inferno; rasterizeTic still works as a two-arg wrapper</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend src/ui/rasterize.test.ts — add IMAGE-03 tests (log scaling, percentile param, colormaps, sentinel)</name>
  <files>src/ui/rasterize.test.ts</files>
  <read_first>
    - src/ui/rasterize.test.ts (read the FULL file — know existing describe blocks, makeGrid helper, rgbaAt helper before adding; do NOT touch existing tests)
    - src/ui/rasterize.ts (just extended — verify exported names to import from)
    - .planning/phases/04-ion-image-intensity-scaling/04-PATTERNS.md lines 228–244 (new describe blocks to add)
  </read_first>
  <behavior>
    - rasterizeImage with logScale false, raw=0 → norm=0 (no NaN)
    - rasterizeImage with logScale true, raw=0 → norm=0 (Math.log1p(0)===0 exactly)
    - rasterizeImage with logScale true, raw=clipMax → norm in [0.99, 1.0] (within floating-point rounding)
    - rasterizeImage with colormap "gray" → R===G===B for every present pixel
    - rasterizeImage with colormap "inferno" → monotonic luminance (later stops brighter than earlier stops — test 3 representative t values)
    - rasterizeImage with colormap "viridis" → sentinel ([0x1a,0x1a,0x1a,255]) at absent cell regardless of opts
    - rasterizeImage with colormap "inferno" → sentinel at absent cell
    - rasterizeImage with colormap "gray" → sentinel at absent cell
    - rasterizeImage with percentile 0.90 → clipMax ≤ clipMax from percentile 0.99 on same values (lower bound)
    - rasterizeTic regression: same output as rasterizeImage(tic, grid, { colormap: "viridis", percentile: 0.99, logScale: false })
  </behavior>
  <action>
    Edit src/ui/rasterize.test.ts by appending new describe blocks AFTER all existing describe blocks. Do NOT modify or remove any existing tests.

    Add imports at the top (alongside existing vitest/rasterizeTic imports): `import { rasterizeImage, inferno, type Colormap, type RasterizeOpts } from "./rasterize"`.

    Add these new describe blocks:

    Block 1 — `describe("rasterizeImage — log scaling")`: Test raw=0 → norm 0 (no NaN in output); test raw=clipMax → pixel is brighter than raw=0 pixel (for colormap "viridis"); test that no pixel in a dense grid has NaN in RGBA channels.

    Block 2 — `describe("rasterizeImage — percentile param")`: Use a 1×4 dense grid with values [10, 50, 90, 100]. Assert that p=0.90 clips at a lower value than p=0.99 (i.e., more pixels are clipped, meaning fewer distinct mid-range outputs — verify via at least one pixel rendering max-brightness under p=0.90 but not under p=0.99).

    Block 3 — `describe("rasterizeImage — inferno colormap")`: Assert monotonic luminance at t=0, 0.5, 1.0 — compute luminance as `0.2126*r + 0.7152*g + 0.0722*b` using the `inferno` export; verify `lum(0) < lum(0.5) < lum(1.0)`.

    Block 4 — `describe("rasterizeImage — gray colormap")`: Create a 1×1 dense grid with a non-zero value. Call rasterizeImage with colormap "gray". Assert R === G === B for that pixel.

    Block 5 — `describe("rasterizeImage — sentinel preserved across colormaps")`: For each of ["viridis", "inferno", "gray"] (iterate or three test cases), create a 1×2 grid with cell 1 absent. Assert that the absent cell always renders as RGBA [0x1a, 0x1a, 0x1a, 255].

    Block 6 — `describe("rasterizeTic — regression wrapper")`: Use the existing makeGrid helper to build a 2×2 dense grid and a Float32Array of known values. Assert that `rasterizeTic(values, grid)` produces the same output as `rasterizeImage(values, grid, { colormap: "viridis", percentile: 0.99, logScale: false })`.

    Reuse the existing `makeGrid` and `rgbaAt` helper functions — do not redefine them.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npx vitest run src/ui/rasterize.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/ui/rasterize.test.ts` exits 0 (all tests pass — existing + new)
    - File contains at least 6 new describe blocks (log scaling, percentile param, inferno, gray, sentinel, regression)
    - File imports rasterizeImage from "./rasterize"
    - No existing test is modified or removed (verify by checking that rasterizeTic describe blocks from Phase 3 are still present)
    - New sentinel test covers all three Colormap values ("viridis", "inferno", "gray")
  </acceptance_criteria>
  <done>All rasterize.test.ts tests pass (existing Phase 3 + new Phase 4 IMAGE-03 coverage); rasterizeImage is verified for log scaling, percentile, colormaps, and sentinel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pure render transform (no I/O) | rasterize.ts functions are DOM-free; they receive Float32Array from the store (which validated inputs at the button handler) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | rasterizeImage log branch (NaN/Inf from Math.log1p) | mitigate | Guard `denom > 0 && raw > 0` before dividing; `Math.log1p(0) === 0` exactly; `Math.log1p` is never negative for non-negative input. Tested by "log scaling → no NaN" test. |
| T-04-04 | Tampering | percentileClip parameter p out-of-range | accept | p is set by the store/UI from a fixed preset dropdown (0.90/0.95/0.99/0.999); never user-typed raw text at this boundary. Math.floor(p*(n-1)) with out-of-range p produces a clamped array index. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages. No supply-chain surface. |
</threat_model>

<verification>
Run after both tasks complete:
```bash
cd /Users/kohlbach/Claude/mzPeakIV && npx vitest run src/ui/rasterize.test.ts && npm run build 2>&1 | tail -5
```
All tests must pass; build exits 0.
</verification>

<success_criteria>
- rasterize.ts exports Colormap, RasterizeOpts, rasterizeImage, inferno alongside existing viridis and rasterizeTic
- rasterizeTic is a thin wrapper — no duplicated pixel loop
- Log scaling uses Math.log1p (Pitfall 4 avoided)
- All existing Phase 3 rasterize tests still pass
- New IMAGE-03 tests cover log, percentile, inferno, gray, sentinel
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/04-ion-image-intensity-scaling/04-02-SUMMARY.md` when done.
</output>
