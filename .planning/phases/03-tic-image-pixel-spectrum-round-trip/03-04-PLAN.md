---
phase: 03-tic-image-pixel-spectrum-round-trip
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND1.log
  - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND2.log
autonomous: false
requirements: [IMAGE-01, IMAGE-04, SPEC-01, SPEC-02, DATA-03]
must_haves:
  truths:
    - "Codex round1 (plan bundle) and round2 (phase diff) have been run for Phase 3"
    - "Each verdict line (accept / accept-with-revisions / reject) is captured for the phase commit footer"
    - "All Phase-3 ROADMAP success criteria are demonstrably met by the full test suite + build"
    - "The reader import boundary holds and no bigint/Arrow types leak into src/compute/ or src/ui/"
  artifacts:
    - path: ".planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND1.log"
      provides: "Adversarial review of the Phase-3 plan bundle"
    - path: ".planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND2.log"
      provides: "Adversarial review of the Phase-3 diff"
  key_links:
    - from: "tools/codex_review.sh"
      to: ".planning/phases/03-tic-image-pixel-spectrum-round-trip/"
      via: "round1/round2 write logs here"
      pattern: "03-CODEX-ROUND"
---

<objective>
Final gate for Phase 3 (PROC-01): run the Codex adversarial review harness against this phase's plan bundle (round1) and the phase diff (round2), capture the verdicts, run the full verification suite, re-assert the reader/compute/ui boundaries, and surface any non-accept verdict to the operator for adjudication.

Purpose: Per CLAUDE.md PROC-01, every phase is bracketed by a Codex CLI adversarial review. This plan exercises `tools/codex_review.sh round1/round2 03` and gates the phase on a green build plus reviewed verdicts. Claude runs the CLI; the operator adjudicates non-accept verdicts. Codex round1 should specifically weigh the two flagged interpretations: (A2) D-07 implemented as "select the array source by representation" rather than exclusive file-handle routing; and (A4 / SPEC-02) the m/z marker deferred to Phase 4 with only an architectural `mzWindow` placeholder shipped now.
Output: Two Codex log files + verdict lines for the phase commit footer; a confirmed-green Phase-3 deliverable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CONTEXT.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-RESEARCH.md

<depends_on_summary>
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-01-SUMMARY.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-02-SUMMARY.md
@.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-03-SUMMARY.md
</depends_on_summary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Phase-3 verification sweep + boundary re-assertion</name>
  <files>(verification only — no source files modified)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 3 Success Criteria #1-#4)
    - .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-01-SUMMARY.md, 03-02-SUMMARY.md, 03-03-SUMMARY.md (what each plan delivered + verify commands)
  </read_first>
  <action>
    Run the complete Phase-3 verification: `npm run lint`, `npx tsc --noEmit`, `npm test` (all Vitest suites incl. compute/tic, ui/rasterize, reader/arrays routing, state/store), and `npm run build`. Run `npx playwright test` only if an imaging fixture/e2e exists; otherwise note that unit coverage on synthetic fixtures is the gate (RESEARCH Pitfall 3 — no imaging `.mzpeak` in `test/data/`) and the visual round-trip is covered by the plan-03 `<human-check>`. Confirm against ROADMAP Phase-3 success criteria: (1) TIC renders as the default spatial overview immediately after grid reconstruction (the eager `'tic'` stage); (2) hover shows 1-based x/y + intensity with fixed orientation `M[row][col]` col=x/row=y (1,1) top-left y-down no-flip and pixel aspect from IMS:1000046/47; (3) clicking a pixel shows that pixel's spectrum from the correct signal file per `MS_1000525` (DATA-03); (4) missing pixels render distinct from zero-intensity via the presence mask. Map each requirement to >=1 passing test: IMAGE-01 -> `src/compute/tic.test.ts`; IMAGE-04 -> tic.test orientation + rasterize sparse-sentinel tests; DATA-03 -> `src/reader/arrays.test.ts` routing block + store rewire; SPEC-01 -> selectSpectrum path + plan-03 human-check; SPEC-02 -> architectural placeholder (mzWindow prop present, no marker — Phase-4 gated; note explicitly). Re-assert the import boundary: `grep -rl mzpeakts src/` returns only files under `src/reader/`. Re-assert no Arrow/mzpeakts runtime leak into `src/compute/` (bigint only via the documented `Number()` boundary conversion). Record any failure as a blocker before invoking Codex.
  </action>
  <verify>
    <automated>cd /Users/kohlbach/Claude/mzPeakIV && npm run lint && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Every command in the verify line exits 0.
    - Each of the four ROADMAP Phase-3 success criteria maps to at least one passing test or build artifact (mapping recorded in the SUMMARY).
    - `grep -rl mzpeakts src/` returns only files under `src/reader/` (boundary held).
    - No file under `src/compute/` imports `apache-arrow` or `mzpeakts` at runtime (one-way boundary held).
    - IMAGE-01, IMAGE-04, SPEC-01, DATA-03 each map to >=1 named passing test; SPEC-02 noted as architecturally enabled (mzWindow placeholder) and Phase-4-gated.
  </acceptance_criteria>
  <done>Full suite green; all four Phase-3 success criteria demonstrably met; the reader/compute boundary re-asserted; requirement-to-test mapping recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Run Codex round1 (plan bundle) and round2 (phase diff)</name>
  <files>.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND1.log, .planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND2.log</files>
  <read_first>
    - CLAUDE.md (PROC-01 section — round1/round2 invocation + verdict-line convention)
    - tools/codex_review.sh (the actual round1/round2 argument handling + where it writes logs)
    - .planning/phases/02-imaging-grid-reconstruction-the-gate/02-04-PLAN.md (how Phase 2 ran the same gate — the analog)
  </read_first>
  <action>
    Determine the phase-start SHA for the diff review: the commit immediately before the first Phase-3 plan landed (inspect `git log --oneline` for the last Phase-2 / pre-Phase-3 commit, e.g. the `docs(03): capture phase context` or the last `docs(02)` commit). Run `bash tools/codex_review.sh round1 03` (adversarial read of the Phase-3 plan bundle, BEFORE-execution semantics — run it now against the four committed plan files) and `bash tools/codex_review.sh round2 03 --sha <phase_start_sha>` (adversarial read of the Phase-3 diff). Capture each verdict line (`accept` / `accept-with-revisions` / `reject`) from the per-round logs at `.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-CODEX-ROUND{1,2}.log` (gitignored per CLAUDE.md). Record both verdict lines in the SUMMARY for copying into the phase commit footer. If either verdict is NOT `accept`, do not silently proceed: summarize the Codex findings (especially any objection to the D-07 representation-routing interpretation or the SPEC-02 deferral) and escalate to the operator for adjudication per PROC-01. On `reject`, stop and surface to the operator.
  </action>
  <verify>
    <human-check>Operator confirms both Codex verdict lines are captured and adjudicated: round1 (plan) and round2 (diff). Any accept-with-revisions or reject is reviewed by the operator before the phase is marked complete. Verdict lines are ready for the phase commit footer.</human-check>
  </verify>
  <acceptance_criteria>
    - `tools/codex_review.sh round1 03` ran and produced `03-CODEX-ROUND1.log` with a verdict line.
    - `tools/codex_review.sh round2 03 --sha <sha>` ran and produced `03-CODEX-ROUND2.log` with a verdict line.
    - Both verdict lines are recorded in `03-04-SUMMARY.md` for the commit footer.
    - Any non-accept verdict is summarized and surfaced to the operator (not silently accepted).
  </acceptance_criteria>
  <done>Both Codex rounds executed for Phase 3; verdicts captured for the commit footer; non-accept verdicts escalated to the operator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local Codex CLI → repo | The review harness reads phase plans + diff; it writes only gitignored log files. No untrusted external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Repudiation | unreviewed phase ships | mitigate | PROC-01 mandates both Codex rounds with verdict lines in the commit footer; this plan is the enforcement gate (`autonomous: false`). |
| T-03-12 | Tampering | non-accept verdict silently ignored | mitigate | A non-accept verdict is summarized and escalated to the operator; `reject` halts the phase. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. slopcheck N/A. |
</threat_model>

<verification>
- Full Phase-3 suite (`npm run lint && npx tsc --noEmit && npm test && npm run build`) green.
- Both Codex logs exist with verdict lines; verdicts recorded for the commit footer.
- Reader/compute import boundary re-asserted.
</verification>

<success_criteria>
- Phase-3 deliverable verified green against all four ROADMAP success criteria.
- Codex round1 + round2 executed; verdicts captured and (if non-accept) adjudicated by the operator.
- DATA-03 / SPEC-02 interpretation calls explicitly put before Codex/operator.
</success_criteria>

<output>
Create `.planning/phases/03-tic-image-pixel-spectrum-round-trip/03-04-SUMMARY.md` when done, including both Codex verdict lines.
</output>
