---
phase: 01-reader-foundation-open-and-inspect
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/reader/openFile.ts
  - src/state/store.ts
  - src/ui/FileLoader.tsx
  - src/ui/ProgressBar.tsx
  - src/ui/MetadataPanel.tsx
  - src/ui/StatsPanel.tsx
  - src/ui/CapabilitiesPanel.tsx
  - src/ui/App.tsx
  - src/reader/stats.ts
autonomous: true
requirements: [LOAD-01, LOAD-03, FMT-01, FMT-02, FMT-03, FMT-04]
must_haves:
  truths:
    - "User can open a local .mzpeak via a file picker AND by drag-and-drop"
    - "User sees staged progress (ZIP index -> manifest -> metadata) with no silent long pause"
    - "User sees the full file-level metadata: file description, instrument config, software, run, sample"
    - "User sees per-file stats: spectrum/entity counts, m/z range if available, MS levels present"
    - "User sees a capabilities readout: point vs chunked layout, encodings present, imaging-detected boolean"
  artifacts:
    - path: "src/reader/openFile.ts"
      provides: "Local File/Blob -> MzPeakReader entry (fromBlob or File-wrapped fromUrl)"
      contains: "File"
    - path: "src/ui/FileLoader.tsx"
      provides: "File picker + drag-and-drop + URL input loader zone"
      contains: "onDrop"
    - path: "src/reader/stats.ts"
      provides: "FileStats + Capabilities computation from eager metadata + manifest"
      contains: "msLevels"
    - path: "src/ui/CapabilitiesPanel.tsx"
      provides: "Layout/encodings/imaging-detected readout"
      contains: "isImaging"
  key_links:
    - from: "src/ui/FileLoader.tsx"
      to: "src/state/store.ts"
      via: "openFile / openUrl actions"
      pattern: "openFile|openUrl"
    - from: "src/ui/CapabilitiesPanel.tsx"
      to: "src/reader/stats.ts"
      via: "store.capabilities"
      pattern: "capabilities"
---

<objective>
Complete the open-and-inspect surface on top of the walking skeleton: add local-file loading (picker + drag-and-drop) alongside the URL path, real staged progress feedback, and the full read-only inspection panels — file-level metadata, per-file stats, and the capability/layout readout (including the imaging-detected boolean probe).

Purpose: Satisfy the "open from local OR URL with staged progress" and "see manifest + metadata + stats + capabilities" success criteria. This is the inspection half of Phase 1's goal; signal-data correctness + fail-loud detection are plan 01-03's concern.
Output: A complete loader zone and a left-side inspection column (manifest + metadata + stats + capabilities).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-reader-foundation-open-and-inspect/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<depends_on_summary>
@.planning/phases/01-reader-foundation-open-and-inspect/01-01-SUMMARY.md
<!-- Uses the reader boundary (openUrl, fileMeta, arrays), the types in src/reader/types.ts,
     and the zustand store from plan 01-01. Read 01-01-SUMMARY for the exact final shapes. -->
</depends_on_summary>

<interfaces>
<!-- Contract this plan extends. Types defined in 01-01's src/reader/types.ts. -->
- store gains: openFile(file: File) action; stage transitions ZIP-index -> manifest -> metadata -> ready.
- src/reader/stats.ts exports: computeStats(reader, manifest) -> FileStats; computeCapabilities(reader, manifest) -> Capabilities.
- Capabilities.unsupported is populated/owned by plan 01-03 (capability detection); this plan DISPLAYS it but its detection logic is 01-03's. Treat unsupported as possibly-empty here; do not implement Numpress/aux/directory detection in this plan (avoid file overlap with 01-03's src/reader/capability.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Local file loading (picker + drag-and-drop) + real staged progress</name>
  <read_first>
    - .planning/phases/01-reader-foundation-open-and-inspect/01-CONTEXT.md (Local: file picker + drag-and-drop via File/Blob; "confirm the reader's local-file entry point; if only fromUrl exists, wrap a File via URL.createObjectURL or extend minimally"; staged progress ZIP index -> manifest -> metadata, LOAD-03)
    - .planning/research/ARCHITECTURE.md (openFile -> MzPeakReader.fromBlob; "a picked File is structured-cloneable"; reader is the single import site)
    - .planning/phases/01-reader-foundation-open-and-inspect/01-01-SUMMARY.md (store shape, existing openUrl, LoadStage type)
    - vendor/mzpeakts/lib/src/reader.ts (confirm whether fromBlob exists; if not, use URL.createObjectURL(file) -> fromUrl)
  </read_first>
  <action>
    Create `src/reader/openFile.ts` (extending the reader boundary — still no mzpeakts import outside src/reader): open a local `File`/`Blob` via `MzPeakReader.fromBlob` if the vendored reader exposes it, otherwise wrap the File with `URL.createObjectURL(file)` and call `fromUrl`, revoking the object URL after the reader finishes indexing. Add an `openFile(file)` action to the store mirroring `openUrl`'s staged transitions. Create `src/ui/FileLoader.tsx` combining: an `<input type="file" accept=".mzpeak">` picker, a drag-and-drop zone (`onDragOver`/`onDrop` accepting the first dropped `.mzpeak`), and the existing URL input. Create `src/ui/ProgressBar.tsx` rendering the current `stage` as labeled steps (ZIP index -> manifest -> metadata -> ready) so there is never a silent long pause (LOAD-03). Wire `FileLoader` + `ProgressBar` into `App.tsx`, replacing the bare URL input from the skeleton.
  </action>
  <verify>
    <automated>npm run build && npx tsc --noEmit && npm test -- src/reader/openFile</automated>
  </verify>
  <acceptance_criteria>
    - `mzpeakts` import gate still holds: `grep -rl "from ['\"]mzpeakts['\"]" src/ | sort -u` returns only files under `src/reader/`.
    - `src/ui/FileLoader.tsx` contains a file `<input>`, an `onDrop` handler, and the URL input; dropping/picking calls `store.openFile`.
    - A Vitest test opens a local fixture `.mzpeak` (read from `test/data/` or a Blob) via `openFile` and asserts `numSpectra > 0`.
    - `ProgressBar` renders distinct labels for each `LoadStage`; store `openFile` transitions through `zip-index`/`manifest`/`metadata`/`ready` (asserted in a store test).
    - `npm run build` and `npx tsc --noEmit` pass.
  </acceptance_criteria>
  <done>User can open a local file via picker and drag-drop, with visible staged progress, in addition to the URL path.</done>
</task>

<task type="auto">
  <name>Task 2: Stats + capabilities computation + full metadata/stats/capabilities panels</name>
  <read_first>
    - .planning/phases/01-reader-foundation-open-and-inspect/01-CONTEXT.md (Metadata & stats decisions: manifest entities; file_description/instrument_configuration_list/software_list/run/sample_list; stats = #spectra, m/z range if available, MS levels; capability readout = point vs chunked, encodings present, imaging-detected boolean probe)
    - .planning/research/ARCHITECTURE.md (eager metadata table: spectrumMetadata.get/dataPointCount/numSpectra; fileMetadata groups)
    - .planning/research/PITFALLS.md (Pitfall 1 note: imaging-detected here is a BOOLEAN probe only — presence of spatial cvParams; full reconstruction is P2; key on accession not column name)
    - vendor/mzpeakts/lib/src/metadata.ts, vendor/mzpeakts/lib/src/array_index.ts (where layout/encoding/CV terms live)
    - src/reader/types.ts (FileStats, Capabilities shapes), src/reader/fileMeta.ts (existing normalizer)
  </read_first>
  <action>
    Create `src/reader/stats.ts` with `computeStats(reader, manifest): FileStats` (numSpectra, numEntities from manifest, m/z range if derivable from metadata/array index, MS levels present from spectrum metadata cvParams) and `computeCapabilities(reader, manifest): Capabilities` populating `layout` (point/chunked/mixed from `spectrum_array_index`/`chunk_encoding`), `encodings` (the CURIEs present), and `isImaging` as a BOOLEAN probe = presence of spatial-position cvParams keyed on accession (IMS:1000050/IMS:1000051) in per-spectrum parameters OR a promoted column — detection only, NO grid reconstruction (that is P2). Leave `capabilities.unsupported` as `[]` here (populated by plan 01-03's detector); the field already exists in the type. Wire `computeStats`/`computeCapabilities` into the store during load (after metadata stage). Expand `src/ui/MetadataPanel.tsx` to render all five metadata groups (file description, instrument configuration list, software list, run, sample list) from the normalized `FileMeta`. Create `src/ui/StatsPanel.tsx` (counts, m/z range if available, MS levels). Create `src/ui/CapabilitiesPanel.tsx` (layout, encodings list, "Imaging data detected: yes/no"). Compose all three into the left inspection column in `App.tsx`.
  </action>
  <verify>
    <automated>npm run build && npm test -- src/reader/stats</automated>
  </verify>
  <acceptance_criteria>
    - `computeStats` against a demo file returns `numSpectra > 0`, `numEntities === manifest.length`, and a `msLevels` array (asserted in a Vitest test).
    - `computeCapabilities` against the point demo returns `layout: 'point'` and an `encodings` array; against the chunked demo returns `layout: 'chunked'` (asserted using both demo fixtures).
    - `isImaging` is computed by probing accession `IMS:1000050`/`IMS:1000051` (grep `IMS:1000050` in `src/reader/stats.ts`); it is a boolean, with NO grid/coordinate reconstruction code in this plan.
    - `MetadataPanel` renders all five FMT-02 groups; `StatsPanel` renders counts + m/z range + MS levels; `CapabilitiesPanel` renders layout + encodings + imaging-detected.
    - `npm run build` passes.
  </acceptance_criteria>
  <done>Left inspection column shows full metadata, per-file stats, and capabilities/layout/imaging-detected for a real file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local file / dropped file -> WASM parser | Untrusted user-selected bytes parsed in-browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-DOS | Denial of Service | drag-drop of a huge/garbage file | accept | Tab-local; main-thread parse acceptable at Phase-1 demo scale; worker offload + size caps are P5. Object URLs are revoked after indexing to avoid leaks. |
| T-01-02-INFO | Information Disclosure | local file never leaves the browser | mitigate | `openFile` uses Blob/object-URL locally; no upload, no fetch of file bytes to any server (privacy is a feature). |
| T-01-02-SC | Tampering | no new package installs in this plan | accept | This plan adds no dependencies; supply-chain surface unchanged from 01-01. |
</threat_model>

<verification>
- `npm run build && npx tsc --noEmit && npm test` pass.
- Local fixture loads via both picker and drag-drop; URL path still works.
- Left column shows manifest + 5 metadata groups + stats + capabilities for both point and chunked demo files.
- `grep -rl "from ['\"]mzpeakts['\"]" src/` still returns only `src/reader/*`.
</verification>

<success_criteria>
- Phase-1 success criteria #1 (local picker + drag-drop + URL, staged progress), #2 (manifest + file/instrument/software/run/sample metadata), #3 (counts + m/z range + MS levels + capabilities readout incl. imaging-detected) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-reader-foundation-open-and-inspect/01-02-SUMMARY.md` when done.
</output>

---

## Round-1 Codex revisions (BINDING — fold into implementation)

> From PROC-01 round1 (verdict: accept-with-revisions). These tighten acceptance; they do not change scope.

- **R-02a (was MAJOR #1): Prove local-file loading in a REAL browser.** Add a Playwright e2e test that loads a bundled local fixture `.mzpeak` through the actual `<input type=file>` (via `page.setInputFiles`) AND via a simulated drag-drop, asserting real metadata renders afterward. A Node/Vitest Blob test alone does NOT satisfy LOAD-01. If `MzPeakReader.fromBlob` does not exist, the `URL.createObjectURL(file) -> fromUrl` path MUST be the one exercised by this browser test (proving zip.js range access works against `blob:` URLs).
- **R-02b (was MAJOR #4): Surface representation in stats/capabilities.** StatsPanel/CapabilitiesPanel MUST show a profile-vs-centroid spectrum breakdown using `SpectrumMeta.representation` from 01-01. **Acceptance:** a test asserts the counts for the demo fixture.
- **R-02c (was MINOR): Imaging detection must be tested, not grepped.** Add a synthetic imaging fixture (promoted `IMS_1000050_position_x`/`IMS_1000051_position_y` columns present) and a non-imaging fixture; assert detection is TRUE via the authoritative columns and reads `metadata.imaging.is_imaging` as a discovery hint, FALSE for non-imaging. Replace the bare `grep IMS:1000050` acceptance with these assertions.
- **R-02d (was MINOR): m/z range explicit states.** For FMT-03, assert a non-null m/z range for a fixture where it is derivable, AND an explicit "m/z range: not available" UI state when it is not. No silent blank.
- **R-02e (was MINOR): Staged progress must be user-visible.** The Playwright load test MUST assert at least one intermediate stage label (e.g. "reading manifest…"/"loading metadata…") is observable during a real load, not merely that the store transitions internally.
