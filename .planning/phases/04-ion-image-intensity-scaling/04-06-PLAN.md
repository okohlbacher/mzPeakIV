---
phase: "04-ion-image-intensity-scaling"
plan: 6
type: execute
wave: 4
depends_on:
  - "04-04"
  - "04-05"
files_modified: []
autonomous: false
requirements:
  - IMAGE-02
  - IMAGE-03
  - SPEC-02
must_haves:
  truths:
    - "Codex round1 adversarial review of the Phase 4 plan is complete and verdict recorded"
    - "Codex round2 adversarial review of the Phase 4 diff is complete and verdict recorded"
    - "Both verdict lines are captured for the operator to copy into the phase commit footer"
  artifacts:
    - path: .planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log
      provides: "Codex round1 review log (gitignored)"
    - path: .planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND2.log
      provides: "Codex round2 review log (gitignored)"
  key_links:
    - from: tools/codex_review.sh
      to: .planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log
      via: "bash tools/codex_review.sh round1 04"
      pattern: "ROUND1.log"
---

<objective>
Run the PROC-01 Codex adversarial review for Phase 4, both rounds:
- round1: adversarial read of the Phase 4 plans (before execution is finalized)
- round2: adversarial read of the Phase 4 diff (after all implementation plans complete)

Purpose: Per CLAUDE.md PROC-01, every phase is bracketed by a Codex adversarial
review. The verdict line must be captured for the operator to adjudicate and copy
into the phase commit footer.

Output: Two log files; human-verify checkpoint presenting both verdict lines.
</objective>

<execution_context>
@/Users/kohlbach/Claude/get-shit-done/workflows/execute-plan.md
@/Users/kohlbach/Claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ion-image-intensity-scaling/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Codex round1 — adversarial review of Phase 4 plans</name>
  <files></files>
  <read_first>
    - tools/codex_review.sh (verify the script exists and the round1 invocation syntax; confirm log path)
    - .planning/phases/04-ion-image-intensity-scaling/ (list plan files to confirm all 5 implementation plans exist before running review)
  </read_first>
  <action>
    Run the Codex round1 review against Phase 4 plans:
    ```
    bash tools/codex_review.sh round1 04
    ```
    The `codex` CLI is at `/opt/homebrew/bin/codex`. The script reads the Phase 4 plan files and the log lands at `.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log` (gitignored per PROC-01).

    After the command completes, extract the verdict line from the log. The verdict line is the line containing `accept`, `accept-with-revisions`, or `reject` (the last such line in the log is authoritative).

    Surface the verdict line to the operator as part of the checkpoint output.
  </action>
  <verify>
    <automated>ls /Users/kohlbach/Claude/mzPeakIV/.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log 2>/dev/null && echo "ROUND1 log exists" || echo "ROUND1 log missing"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log` exists and is non-empty
    - The log contains a verdict line matching `accept`, `accept-with-revisions`, or `reject`
    - The verdict line is surfaced in the checkpoint output for operator review
  </acceptance_criteria>
  <done>Codex round1 log written; verdict line extracted and displayed</done>
</task>

<task type="auto">
  <name>Task 2: Run Codex round2 — adversarial review of Phase 4 diff</name>
  <files></files>
  <read_first>
    - tools/codex_review.sh (verify round2 invocation syntax with --sha argument)
    - The git log to identify the SHA at the start of Phase 4 implementation (the commit just before Plan 01 execution began)
  </read_first>
  <action>
    Identify the SHA at the start of Phase 4 (the last commit before any Phase 4 code was written):
    ```
    git log --oneline -10
    ```
    Select the SHA of the commit just before any Phase 4 implementation files were created (look for the Phase 4 planning commit: "docs(04): ...").

    Then run the Codex round2 review against the Phase 4 diff:
    ```
    bash tools/codex_review.sh round2 04 --sha <phase_start_sha>
    ```

    The log lands at `.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND2.log`.

    After the command completes, extract the verdict line and surface it to the operator.
  </action>
  <verify>
    <automated>ls /Users/kohlbach/Claude/mzPeakIV/.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND2.log 2>/dev/null && echo "ROUND2 log exists" || echo "ROUND2 log missing"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND2.log` exists and is non-empty
    - The log contains a verdict line matching `accept`, `accept-with-revisions`, or `reject`
    - The verdict line is surfaced in the checkpoint output for operator review
  </acceptance_criteria>
  <done>Codex round2 log written; verdict line extracted and displayed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Both Codex adversarial reviews have been run:
    - round1 (plan review): verdict extracted from 04-CODEX-ROUND1.log
    - round2 (diff review): verdict extracted from 04-CODEX-ROUND2.log
  </what-built>
  <how-to-verify>
    1. Review the round1 verdict line from 04-CODEX-ROUND1.log
    2. Review the round2 verdict line from 04-CODEX-ROUND2.log
    3. If verdict is `accept`: copy both verdict lines into the phase commit footer and proceed to close Phase 4
    4. If verdict is `accept-with-revisions`: review the substantive items in the log, adjudicate (address or consciously defer each item), copy the verdict line into the commit footer, and note the adjudication outcome in the commit message
    5. If verdict is `reject`: escalate — do not close Phase 4 until root concerns are addressed
    6. The phase commit footer format per PROC-01: append both verdict lines as trailer lines, e.g.:
       ```
       Codex-Round1: accept-with-revisions
       Codex-Round2: accept
       ```
  </how-to-verify>
  <resume-signal>Type "approved" once both verdict lines are noted and any adjudication decisions are made</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Codex CLI invocation | codex at /opt/homebrew/bin/codex reads plan files and git diff; no file write beyond the log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Information Disclosure | Codex review log | accept | Logs are gitignored per PROC-01; they contain the adversarial review output (plan/diff content). Appropriate for a local developer tool. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. codex CLI is pre-existing at /opt/homebrew/bin/codex. |
</threat_model>

<verification>
Both log files exist and contain verdict lines:
```bash
grep -i "accept\|reject" /Users/kohlbach/Claude/mzPeakIV/.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND1.log | tail -1
grep -i "accept\|reject" /Users/kohlbach/Claude/mzPeakIV/.planning/phases/04-ion-image-intensity-scaling/04-CODEX-ROUND2.log | tail -1
```
</verification>

<success_criteria>
- Both Codex review logs exist and contain a verdict line
- Operator has reviewed both verdicts and noted adjudication decisions
- Phase commit footer will include both verdict lines per PROC-01
</success_criteria>

<output>
Create `.planning/phases/04-ion-image-intensity-scaling/04-06-SUMMARY.md` when done.
</output>
