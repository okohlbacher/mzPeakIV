**Panel** — collapsible titled section; the building block of the inspector rail.

```jsx
<Panel title="Image Info" count="5">
  <StatRow label="Spectra" value="1,684" />
</Panel>
```

Uncontrolled via `defaultOpen`, or controlled with `open` + `onToggle`. Titles render as uppercase overlines.
